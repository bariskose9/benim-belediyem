# Yol Haritası

Sıra kasıtlıdır: her adım bir öncekinin üzerine kurulur. Bir adım
`docs/standards/10-definition-of-done.md` kapılarını geçmeden sonrakine geçilmez.

| # | Adım | Çıktı |
|---|---|---|
| 0 | Repo, Next.js, TypeScript, Tailwind, ESLint/Prettier, docs/, CLAUDE.md | Boş proje ayakta |
| 1 | GitHub + Vercel + Neon bağlantısı, `/api/health`, CI pipeline | **Canlı boş sayfa** |
| 2 | Docker Compose (local Postgres), Prisma kurulumu, ilk migration | Local DB çalışıyor |
| 3 | ~~Veri modeli + tüm tablolar + `fake-data-guide.md`'ye uygun seed~~ **BİTTİ** | 37 tablo + idempotent seed |
| 4a | ~~Sahte KPS servisi: `kps_citizens` seed, mock uç, gecikme/hata simülasyonu, hız sınırı~~ **BİTTİ** | KPS sorgusu çalışıyor (ADR-009, ADR-010) |
| 4b-1 | ~~Kayıt: TCKN + doğum yılı → KPS → **18 yaş kontrolü** → iki bağımsız OTP · `OtpChannel` adaptörü · **bot koruması** (ADR-004) · argon2id şifre (ADR-011) · kayıt taslağı (ADR-012)~~ **BİTTİ** | Kayıt çalışıyor |
| 4b-2 | ~~Giriş, çıkış, **veritabanı oturumu** (ADR-005), rol, korumalı route~~ **BİTTİ** | Giriş çalışıyor |
| 4b-3 | ~~Şifre sıfırlama + hesap sayımı koruması (sahte kod kaydı · sabit yanıt süresi · şifre değişince tüm oturumlar düşer)~~ **BİTTİ** | Şifre sıfırlama çalışıyor |
| 4c | ~~Google ile giriş (OAuth): PKCE + `state` + `nonce` · hesap birleştirme kuralı · doğrulanmamış hesap kademesi~~ **BİTTİ** | Google ile giriş çalışıyor |
| 5 | ~~Layout: navbar, logo, dark mode, responsive iskelet, tasarım token'ları~~ **BİTTİ** | Görsel iskelet çalışıyor (kendi kelime-logomuz, lacivert+turkuaz palet, tema düğmesi, mobil menü, alt bilgi, hizmet ızgarası) |
| 6 | ~~Hastane randevu modülü (personele özel): branş → doktor → gün → saat · dört iş kuralı sunucuda · koşullu UPDATE ile çift satış koruması~~ **BİTTİ** | Randevu alma ve iptal çalışıyor |
| 7 | ~~Ortak sepet + sahte kart ödemesi + kayıtlı kart altyapısı: ziyaretçi sepeti ve girişte birleştirme · modül başına teslimat ücreti · Luhn + sahte sağlayıcı · tek transaction'da ödeme + modül başına sipariş + stok düşümü~~ **BİTTİ** | Ödeme çalışıyor |
| 8 | ~~Belediye Market: görselli/kategorili/stoklu ürün ızgarası · arama ve kategori süzgeci (adreste taşınır) · hazır sepet ucuna bağlanan "sepete ekle" · tükenmiş ürün listede kalır ama eklenemez~~ **BİTTİ** | Market çalışıyor |
| 9 | ~~Belediye Restoran + adisyon: görselli/kategorili menü · arama ve kategori süzgeci (market ile ORTAK katmandan) · adet + mutfak notuyla adisyona ekleme · adisyon = sepetin restoran bölümü, kalıcı · restoran teslimatı adres + tahmini hazırlık süresi (zaman aralığı YOK)~~ **BİTTİ** | Restoran ve adisyon çalışıyor |
| 10 | ~~Sipariş takibi + bildirim sistemi: durum okuma anında türetilir (ADR-013) · uygulama içi bildirim (tembel, tekrarlanamaz) · iptal yalnızca `Alındı` aşamasında, tek transaction'da stok iadesi + sahte iade kaydı + denetim kaydı~~ **BİTTİ** | Sipariş takibi ve iptali çalışıyor |
| 11 | ~~Etkinlik + koltuk seçimi + bilet: salon planı · **10 dakikalık koltuk kilidi** (tek ifadeli koşullu yazma + `unique(event_id, seat_id)`) · süresi dolan kilit okuma anında yok sayılır (ADR-007) · sepette geri sayım ve otomatik düşme · ödeme transaction'ında `held → sold`~~ **BİTTİ** | Etkinlik bileti çalışıyor |
| 12 | ~~Spor salonu üyeliği (personele özel): tesis + ders programı · dört paket, **hepsi aylık tahsilat** · üyelik sepete girmez, kendi akışı · ilk ay tahsilatı · paket değişimi bir sonraki vadede · erken çıkış farkı · yenileme hatırlatması (tembel) · durum okuma anında türetilir~~ **BİTTİ** | Spor salonu üyeliği çalışıyor |
| 13 | ~~Destek talebi + dosya yükleme: durum okuma anında türetilir (ADR-013) · `Kapandı` yalnızca talebi açan üyeden · dosya türü BAYT İMZASINDAN doğrulanır, en fazla 5 × 2 MB · ekler yetkili uçtan servis edilir (ADR-014) · başkasının talebi ve eki 404~~ **BİTTİ** | Destek talebi çalışıyor |
| 14 | ~~Bilgi widget'ları (hava, haber, piyasa): dört sağlayıcının dördü de ANAHTARSIZ · çağrılar sunucuda, zaman aşımı + 2 deneme + sağlayıcı başına devre kesici · yanıtlar Postgres'te önbellekleniyor ve sağlayıcı çökünce 24 saate kadar BAYAT veri "güncellenemiyor" notuyla sunuluyor (ADR-015) · haber anahtarsız RSS + kendi minimal okuyucumuz, bağlantılar alan adı beyaz listesinden geçiyor (ADR-016)~~ **BİTTİ** | Bilgi panosu çalışıyor |
| 15 | ~~Profil sayfası: `/hesabim` bir merkeze dönüştü (kayıtlarım + hesap ayarları) · teslimat adresi YÖNETİMİ (listele/ekle/düzenle/sil) · kayıtlı kart listeleme ve kaldırma · iki silme de YUMUŞAK (geçmiş sipariş ve mali kayıt bozulmuyor) · sahiplik sorgunun `WHERE` koşulunda (başkasınınki 404) · üyeliğe bağlı kart kaldırılırken uyarı · adres/kart yazmaları denetim kaydına düşüyor~~ **BİTTİ** | Profil sayfası çalışıyor |
| 15c-1 | ~~Profilden Google bağlantısı ekleme/kaldırma (borç #33): giriş yöntemleri kartı · bağlama ŞİFRE istiyor (çalınmış oturum tek başına yetmesin) · SON giriş yöntemi kaldırılamaz · aynı Google hesabı iki kullanıcıya bağlanamaz · OAuth akışına "bağlama" modu, mod istemciden GELMİYOR · callback akışı başlatan kullanıcıyı doğruluyor · iki denetim işlemi, Google kimliği kayda yazılmıyor~~ **BİTTİ** | Google bağlantısı çalışıyor |
| 15c-2 | ~~Google ile girenin KPS doğrulaması (borç #32): `/kimlik-dogrulama` ekranı · sıra bot kapısı → KPS → 18 yaş → "bu numara başka hesapta mı" → personel eşleştirme · yazma TEK KOŞULLU (yarış koruması ölçüldü) · ad soyad KPS'ten geliyor, Google'ın geçici adı gidiyor · erişim uyarısındaki bağlantı artık kayıt ekranına değil doğrulama ekranına gidiyor ve kullanıcıyı geldiği hizmete geri döndürüyor~~ **BİTTİ** | Kimlik doğrulama çalışıyor ve CANLIDA (`65fc6e1`) |
| 15b | ~~Hakkımızda: teşkilat şeması + 100 kişilik personel rehberi · ziyaretçiye açık · 35 birim TEK sorguda okunup bellekte ağaca dönüşüyor · açılır kapanır `<details>` ağacı · birim seçimi alt birimleri de kapsıyor · ada göre aksan körü arama + birim/unvan süzgeci · personeli olmayan daireler ismen duruyor ve "henüz yayınlanmadı" diyor · kimlik özeti sorguda HİÇ okunmuyor (üç katman test)~~ **BİTTİ** | Hakkımızda çalışıyor |
| 16 | ~~Planlı görevler: günlük `GET /api/cron/daily` (`CRON_SECRET` kapısı, sabit süreli karşılaştırma) · dokuz bağımsız görev, biri patlarsa diğerleri sürüyor · her görev ayrı denetim kaydı yazıyor · temizlikler "payla" siliyor (24 saat), dış veri önbelleğinde ölçüt `fetchedAt` çünkü bayat yedek korunmalı (ADR-015) · aidat tahsilatı ve takvim uzatması İDEMPOTENT, ikisi de ölçüldü · doktor takvimi her gün 14 güne tamamlanıyor · "durum simülasyonu" GEREKMEDİ: durumlar zaten okuma anında türetiliyor (ADR-013)~~ **BİTTİ** | Planlı görevler çalışıyor |
| 17 | ~~Yasal sayfalar + çerez rızası: dört belge (`/gizlilik` · `/cerez-politikasi` · `/kullanim-sartlari` · `/iletisim`), alt bilgiden erişilir · çerez listesi TEK KAYNAKTAN (`cookie-registry.ts`) üretiliyor, elle yazılmıyor · bant **SIFIR JAVASCRIPT** (düz form POST + 303) ve rızası olana hiç çizilmiyor · bant "kabul et/reddet" DEĞİL bilgilendirme, çünkü zorunlu olmayan çerez YOK — kataloğa `analytics` satırı eklenirse test kırmızıya dönüyor · rıza `consent_records`'a eklemeli (append-only) yazılıyor, geri alma eski satırı değiştirmiyor · ziyaretçi rızası `anonymousId`'ye bağlanıyor, girişte hesaba TARİHİ KAYMADAN taşınıyor · veri sorumlusu kişisel verisi koda değil ORTAM DEĞİŞKENİNE yazıldı · uçta CSRF kapısı (`Origin`) ve açık yönlendirme koruması~~ **BİTTİ** | Yasal sayfalar çalışıyor ve CANLIDA (`5b1f814`) |
| 17b | ~~Hesap yönetimi: `/hesabim/verilerim` · verimi indir (JSON, dokuz bölüm, şifre özeti/oturum jetonu/şifreli kimlik HİÇ okunmuyor) · hesabımı sil (kişisel alanlar SİLİNİR, mali kayıtlar TTK m.82 gereği 10 yıl kişiselleştirilmeden saklanır) · neyin saklandığı ve hangi kanun gereği saklandığı onaydan ÖNCE ve `/hesap-silindi` sayfasında BİLDİRİLİYOR (Yönetmelik m.12/1-c) · silmenin denetim kaydı hesapla birlikte silinmiyor (m.7/3) · üyelik iptal edilmiyor, otomatik yenilemesi kapatılıyor (iptal erken çıkış farkı tahsil ederdi) · kimlik bağlantısını çözme (ADR-017 ilke 3) ve o çözmenin hesabı kilitlemesini engelleyen kural · telefon güncelleme, numara DOĞRULANMAMIŞ yazılıyor~~ **BİTTİ** | Hesap yönetimi çalışıyor |
| 17c | ~~Personel yetkisini kimlik doğrulamasından ayır (ADR-017 ilke 2): `matchStaffMember` SİLİNDİ, `isStaff` ve `staffMemberId` kayıt akışının ve kimlik doğrulamanın yazma girdilerinden ÇIKARILDI (iki yerden birden — biri kalsaydı kapı o taraftan açık kalırdı) · yeni `/personel-dogrulama` ekranı ve `src/features/staff-verification/` katmanı · kod kullanıcının kendi adresine DEĞİL `staff_members.work_email` adresine gidiyor (kanıt "bildiğin" değil "sahip olduğun") · ön koşul `kps_verified` — kimlik yetki VERMEZ ama yetkinin ÖN KOŞULUDUR · ikinci adımda hedef bağlama kontrolü (kodu başka bir personelin adresiyle kullanmak engelleniyor) · rehberde yok / işten ayrılmış / zaten bağlı üçü de SAHTE akışa düşüyor ve aynı cümleyi üretiyor · hız sınırı gerçek/sahte ayrımından ÖNCE tükeniyor · bağlama tek koşullu yazma, son sözü `users.staff_member_id` benzersizlik kısıtı söylüyor · `role_change` denetim kaydı~~ **BİTTİ** | Personel doğrulaması çalışıyor |
| 18a | ~~Gözlemlenebilirlik: yapılandırılmış (JSON) log katmanı · TEK kişisel veri süzgeci (`redact`) hem log'a hem Sentry'ye uygulanıyor · süzgeç iki bağımsız savunma taşıyor (alan adı + değerin BİÇİMİ) çünkü Prisma kişisel veriyi hata METNİNİN içine koyuyor · 32 düz metin `console.error` logger'a taşındı ve `no-console` istisnasız yasaklandı · Sentry SDK, olaylar `tunnelRoute` ile KENDİ alan adımızdan geçiyor (CSP'ye dokunulmadı, reklam engelleyici düşüremiyor) · SDK'nın veri toplama VARSAYILANLARI kapatıldı (istek gövdesi, çerez, başlık, DB parametreleri, yığın değişkenleri) · oturum tekrarı BİLEREK kurulmadı · `logger.error`/`warn` bir sink üzerinden Sentry'ye gidiyor, yani YAKALANMIŞ arızalar da görünüyor · kök hata sınırı (`global-error.tsx`) geldi~~ **BİTTİ** | Hata görünürlüğü çalışıyor · borç #79 ÖDENDİ |
| 18b | ~~Swagger / OpenAPI (`/api/docs`) — 46 ucun tamamı belgelendi · belge ELLE YAZILMIYOR, gerçek Zod şemalarından türetiliyor (**yeni paket eklenmedi** — Zod 4'ün `toJSONSchema`'sı 42/42 şemayı dönüştürdü, ölçüldü) · OpenAPI **3.1** (JSON Schema 2020-12 ile birebir; 3.0 sessizce kayıplı olurdu) · sapma **CI kapısı**: uç, hayalet uç, **erişim seviyesi** ve hata kataloğu dört ayrı testle gerçeğe bağlı · ⭐ erişim testi bir MUTASYON deneyinden doğdu (etiket `authenticated`→`public` yapıldı, testler yeşil kalmıştı) · gizlilik kapısı adım 18a'nın süzgecini kullanıyor · belge production'da **varsayılan KAPALI** (ADR-019)~~ **BİTTİ** | API belgesi · borç #105-#107 bulundu |
| 18c | ~~Performans bütçesi + erişilebilirlik kapısı CI'da fiilen ÖLÇÜLÜYOR (`tests/quality/`, ayrı `quality` Playwright projesi) · ilk yük JS (gzip), LCP ve CLS gerçek Chrome'da 4 kat CPU kısıtıyla ölçülüyor · WCAG 2.1 AA denetimi 13 genel sayfada (`@axe-core/playwright`) · ⭐ borç #84'ün YAZILI SEBEBİ mutasyonla YANLIŞLANDI: bant tek başına sebep değil, `SiteHeader` adım 4a'dan beri aynı şeyi yapıyor · ⭐ ilk yazılan bütçe listesi SESSİZCE GİRİŞ SAYFASINI ölçüyordu (`/spor-salonu` personele özel, yönlendirme hidrasyondan sonra) → `expectRoute` kapısı doğdu · iki kapı da mutasyonla kırmızıya döndürülerek kanıtlandı · **lighthouse paketi GEREKMEDİ** (Playwright + Chrome protokolü yetti)~~ **BİTTİ** | Ölçülen bütçe · borç #11 ÖDENDİ · #84 KAPANDI · #108-#109 doğdu |
| 18d | ~~Güvenlik denetimi raporu (`docs/project/guvenlik-denetimi-2026-08.md`) — OWASP Top 10 madde madde ÖLÇÜLEREK denetlendi · **borç #10 ÖDENDİ:** nonce tabanlı sıkı CSP (`src/proxy.ts`, `script-src`'den `'unsafe-inline'` KALKTI) ve ⭐ maliyeti ölçüldü, SIFIR çıktı (statik rota 3→3, dinamik 77→77 — Next belgesinin uyardığı "statik render kaybı" bu projede zaten yoktu) · **borç #78 ÖDENDİ ve ENDİŞESİ YANLIŞ ÇIKTI** (Vercel `x-forwarded-for`'u üzerine yazıyor, spoof edilemiyor; kaydın önerdiği çözüm de etkisiz olurdu) · **borç #99 ÖDENDİ** (11/11 action tam SHA'ya sabitlendi; `permissions:` blokları ZATEN VARDI) · güvenlik başlıkları bugüne kadar HİÇ test edilmiyordu, 19 testlik kapı yazıldı · üç kapı da mutasyonla kırmızıya döndürülerek kanıtlandı · **#23 ve #89 sahibin kararına bırakıldı**~~ **BİTTİ** | Üretime hazır · #10 · #78 · #99 ÖDENDİ · #112-#113 doğdu |
| 19 | Expo mobil uygulama (aynı API) | Mobil |

> ⛔ **GERÇEK KİŞİSEL VERİYLE ÇALIŞTIRMA KAPISI (ADR-017):** adım **17b**'nin
> kimlik çözme akışı, adım **17c**'nin yetki ayrımı ve **gerçek kimlik
> sağlayıcısı** (e-Devlet / banka doğrulaması) üçü birden bitmeden bu uygulama
> gerçek vatandaş verisiyle çalıştırılamaz. Sahte KPS ile portföy/gösterim
> kullanımı bu kapının dışındadır.

## Teknik borç
Buraya sadece **bilinen ve kabul edilmiş** eksikler yazılır. Boş bırakılmaz, gizlenmez.

| # | Borç | Neden kabul edildi | Ne zaman ödenir |
|---|---|---|---|
| 1 | **Telefon doğrulaması SİMÜLE EDİLİYOR — gerçek SMS sağlayıcısı eklenene kadar güvenlik katkısı YOK.** Kod telefona değil kullanıcının e-postasına gidiyor; yani numaranın kullanıcıya ait olduğu kanıtlanmıyor | Türkiye'de gerçek SMS ücretli ve İYS/marka onayı istiyor (PRD §2 kapsam dışı). Akış, veri modeli ve hız sınırları doğru kurulsun diye adım yine de var. Simülasyon olduğu ekranda ve e-postada AÇIKÇA yazıyor | Gerçek SMS sağlayıcısı eklenirse — yalnızca `OtpChannel`'ın yeni bir uygulaması yazılır, akış değişmez |
| 2 | **Hız sınırı Postgres'te** — Redis'ten yavaş | Ek servis, hesap ve yapılandırma maliyeti bu ölçekte gereksiz (ADR-006) | Ölçüm darboğaz gösterirse |
| 3 | **Sık çalışan cron yok** — ücretsiz planda günde 1 | Doğruluk okuma anında sağlanıyor, cron yalnızca temizlik yapıyor (ADR-007) | Ücretli plana geçilirse |
| 4 | **Yönetici paneli yok** — sipariş ve destek durumları zamanlayıcıyla simüle ediliyor | Faz 2'ye bırakıldı (PRD §2) | Faz 2 |
| 5 | **Veritabanı oturumu her istekte bir okuma yapıyor** | Anında iptal edilebilirlik için bilinçli tercih (ADR-005) | p95 yanıt süresi SLO'yu zorlarsa |
| 6 | **Bot koruması ABD merkezli servise bağlı** (Cloudflare) | Ücretsiz ve çerezsiz tek makul seçenek (ADR-004) | Yurt içi işleyici zorunluluğu doğarsa |
| 7 | **`tailwind.config.ts` yok** — Tailwind v4 CSS-first yapılandırma | v4 bu dosyayı üretmiyor; token'lar `globals.css` içinde. v3'e dönmek projeyi eski sürümle başlatmak olurdu | Ödenmesi gerekmiyor — `REPO-YAPISI.md` güncellendi |
| 8 | **TypeScript 6 kullanılıyor, 7 değil** | `typescript-eslint` TS 7'yi desteklemiyor (peer `<6.1.0`); TS 7'ye çıkmak lint kapısını devre dışı bırakırdı | `typescript-eslint` TS 7 desteği yayınlayınca |
| 9 | **ESLint 9 kullanılıyor, 10 değil** | `eslint-config-next`'in bağımlılıkları (`eslint-plugin-import`, `jsx-a11y`) en fazla 9 kabul ediyor | Next.js bu eklentileri ESLint 10'a taşıyınca |
| ~~10~~ | ~~**Sıkı (nonce tabanlı) CSP yok** — baseline başlıklar var~~ | **ÖDENDİ (2026-08-12, adım 18d):** politika `next.config.ts`'ten `src/proxy.ts`'e taşındı (⚠️ Next 16'da ara katmanın adı `middleware` değil **`proxy`**) ve her istekte yeni bir nonce üretiyor. `script-src`'den **`'unsafe-inline'` kalktı**, yerine `'nonce-…' 'strict-dynamic'` geldi. ⭐ **Korkulan bedel ÖLÇÜLDÜ ve SIFIR çıktı:** Next belgesi "tüm sayfalar dinamik olur, statik render ve CDN önbelleği kaybolur" diye uyarıyor, ama build çıktısı ölçüldü — statik rota 3→3, dinamik 77→77 DEĞİŞMEDİ. Sebebi borç #84'te yazılı: `SiteHeader` oturum çerezini adım 4a'dan beri okuyor, yani kaybedilecek statik render zaten yoktu. Tarayıcıda doğrulandı: nonce her istekte farklı, çalıştırılabilir betiklerde nonce'suz 0, tema betiği çalışıyor, Turnstile betiği `strict-dynamic` altında yükleniyor. 19 testlik kapı (`tests/e2e/guvenlik-basliklari.spec.ts`) altı ayrı yolda ölçüyor ve mutasyonla kanıtlandı | — |
| ~~11~~ | ~~**CI'da `bundle-size` / `lighthouse` / `axe` kapıları yok**~~ | **ÖDENDİ (2026-08-11, adım 18c):** üçü de `tests/quality/` altında ve CI'da ayrı bir adım olarak koşuyor. `bundle-size` ilk yük JS'i gzip hâliyle telden ölçüyor, `lighthouse` yerine Playwright + Chrome protokolü kullanıldı (**yeni paket gerekmedi** — ölçüldü), `axe` WCAG 2.1 AA kurallarını 13 genel sayfada denetliyor. İkisi de mutasyonla kırmızıya döndürülerek kanıtlandı. ⚠️ Eşiklerin bir kısmı bugünkü ölçüme kilitli (cırcır) — sebebi #108 ve #109 | — |
| ~~12~~ | ~~**`npm audit` geliştirme bağımlılıklarında 9 yüksek uyarı veriyor**~~ | **ÖDENDİ (2026-08-02):** `npm audit` ve `npm audit --omit=dev` ikisi de **0 açık** bildiriyor. Bağımlılık listesi bu adımda değiştirilmedi; uyarının kaybolması muhtemelen upstream'de yamalı sürümün yayınlanmasından — **emin değilim**, tek bildiğim komutun bugün temiz döndüğü | — |
| 13 | **`prisma.config.ts` Prisma'nın `env()` yardımcısını kullanmıyor** | `env()` değişkeni yapılandırma yüklenirken zorunlu kılıyor ve veritabanına hiç bağlanmayan `prisma generate` komutunu bile başarısız kılıyor (Docker derlemesi ve Vercel `postinstall` bu yüzden patlıyordu). Eksik değişken zaten `src/config/env.ts` tarafından açılışta yakalanıyor | Prisma bu davranışı düzeltirse |
| ~~14~~ | ~~**Docker imajı yerel makinede doğrulanamadı**~~ | **ÖDENDİ (2026-07-31):** imaj derlendi (264MB), kap `healthy` durumuna geçti, root olmayan `nextjs` kullanıcısıyla çalıştı, `/api/health` veritabanına bağlanıp `db: ok` döndü, anasayfa 200 verdi. İmaj ayrıca her PR'da CI'da derleniyor | — |
| ~~15~~ | ~~**Örnek üye hesaplarının şifresi yazılmıyor**~~ | **ÖDENDİ (2026-08-01):** argon2id seçildi (ADR-011) ve 10 demo hesabına özet yazıldı. Arka plandaki 80 hesabın şifresi bilerek yok — giriş yapmaları beklenmiyor, 80 gereksiz özet tohumlamayı ~8 sn uzatırdı | — |
| 16 | **Ürün görselleri kategori başına tek yer tutucu** | 45 ürün + 31 menü kalemi için ayrı görsel üretmek, ekran olmadan doğrulanamayacak 76 dosya demekti. Kategori başına üretilmiş SVG yer tutucu (11 dosya) 404 vermez ve `alt` metni taşır. **Adım 8'de ÖDENMEDİ:** market ızgarası yer tutucularla açıldı; 45 ürüne ayrı görsel üretmek ekranın kendisinden büyük bir işti ve görseller olmadan da ızgara, süzgeç ve sepete ekleme akışı tam olarak doğrulanabildi | Gerçek ürün görselleri temin edilirse — kod değişikliği gerekmiyor, yalnızca `Product.imageUrl` değerleri ve dosyalar değişir |
| 17 | **Dolu doktor slotlarının çoğunda randevu kaydı yok** | Rehber slotların %30'unu dolu istiyor (~1960 slot). Hepsine gerçek randevu yazmak, tek kullanıcıya yüzlerce randevu vermek ve "aynı branşta aynı gün tek randevu" kuralını (PRD §5.1) seed'in kendisinde ihlal etmek olurdu. Dolu slotların bir kısmı sahipli, geri kalanı "başkasının randevusu" — uygulama zaten başkasının randevusunu göstermez | Ödenmesi gerekmiyor — bilinçli tasarım |
| ~~18~~ | ~~**`rate_limit_counters` tablosu hiç temizlenmiyor**~~ | **ÖDENDİ (2026-08-10, adım 16):** `cleanup_rate_limits` görevi geldi. Hız sınırı ve devre kesici satırlarının ikisini birden kapsıyor — ikisi aynı tabloda yaşıyor (ADR-010, `circuit:` ön eki) ve ikisi de yalnızca kendi penceresi içinde okunuyor, yani eski satır ikisinde de ölü. Silme 24 saatlik payla yapılıyor; pay kaldırılıp testin kırmızıya döndüğü görüldü | — |
| ~~19~~ | ~~**`MockKpsProvider` henüz hiçbir uçtan çağrılmıyor**~~ | **ÖDENDİ (2026-08-01):** `POST /api/registrations` `lookupIdentity()`'yi çağırıyor; kayıt ekranı tarayıcıda uçtan uca doğrulandı | — |
| ~~21~~ | ~~**Preview'da bot koruması test anahtarlarıyla**~~ | **ÖDENDİ (2026-08-01):** Cloudflare hesabı açıldı, `benim-belediyem` widget'ı Managed modda kuruldu, gerçek anahtar çifti preview ve production'a girildi. Canlıda gerçek onay kutusu çıktığı fiilen doğrulandı | Proje sahibinin henüz Cloudflare hesabı yok. Test anahtarları gerçek `siteverify` çağrısını yaptırıyor, yani kod yolu fiilen çalışıyor ve preview tıklanarak test edilebiliyor; yalnızca kapı her zaman "geç" diyor | Cloudflare hesabı açılıp preview'a gerçek anahtar çifti girilince (~5 dk) |
| ~~22~~ | ~~**Production'da kayıt KAPALI**~~ | **ÖDENDİ (2026-08-01):** Resend hesabı açıldı, gönderim yetkili anahtar production'a girildi, kayıt ekranı canlıda açıldı. **Kalan sınır:** doğrulanmış alan adı olmadığı için Resend yalnızca hesabın kayıtlı e-postasına gönderiyor → borç #25 | Production'da kod ekranda gösterilemez (05-auth-security.md), gönderecek servis de yok. Kullanıcıya kimliğini doğrulatıp hiç gelmeyecek bir kodu beklettirmek yerine akış en baştan kapatılıyor ve sebebi ekranda yazıyor. Uygulamanın geri kalanı ayakta | Resend hesabı açılıp `EMAIL_API_KEY` + `EMAIL_FROM` girilince |
| 23 | **Sızmış şifre kontrolü kısa yerel listeyle yapılıyor** (~120 şifre) | Gerçek sızıntı veri tabanı (HIBP range API) ABD merkezli üçüncü bir işleyici demek: `integrations.md` ve KVKK aydınlatma metnine yeni satır, ayrıca servis çökünce ne yapılacağı sorusu. Yayımlanmış liste dosyalarının lisansı da herkese açık bir depoya kopyalanmadan önce netleşmeli. Bugünkü liste yalnızca en bariz tercihleri (`12345678`, `sifre123`, `galatasaray`) engelliyor | Adım 18 (güvenlik denetimi) |
| 24 | **Bot koruması E2E testlerinde kapalı** | Her E2E koşusunun Cloudflare'a ağdan bağımlı olması testleri kararsızlaştırırdı. Kapının kendisi unit ve entegrasyon testlerinde kapsanıyor, ayrıca tarayıcıda elle doğrulandı | Ödenmesi gerekmiyor — bilinçli takas, `playwright.config.ts` içinde yazılı |
| 25 | **Canlıda kayıt yalnızca proje sahibinin e-postasıyla tamamlanabilir** | Resend'de doğrulanmış alan adı yok; ücretsiz katman bu durumda sadece hesabın kayıtlı adresine gönderiyor. Başka bir adresle kayıt olan kullanıcı kodu hiç alamaz. Portföy/gösterim amacı için yeterli, gerçek kullanıma açılırsa engel | Alan adı satın alınıp Resend'de doğrulanınca |
| 26 | **Oturum çerezi tarayıcıda uzamıyor** — veritabanındaki oturum kayan yenilemeyle uzuyor ama çerezin `maxAge`'i yalnızca giriş anında ayarlanıyor. Yalnızca sayfa okuyup 7 gün boyunca hiçbir istek göndermeyen kullanıcı yine de yeniden giriş yapar | Next.js sunucu bileşenlerinde çerez YAZILAMAZ; okuma sırasında tazelemek mümkün değil. Düzeltmesi Node çalışma zamanlı bir ara katman (middleware) gerektiriyor — Prisma sürücü adaptörü Edge'de çalışmadığı için (ADR-008) bu ayrı bir iş | Ara katman eklenirse veya kullanıcıdan gerçek şikâyet gelirse |
| 27 | **Üst menü her sayfayı dinamik yapıyor** — `SiteHeader` oturumu okumak için `cookies()` çağırıyor, dolayısıyla hiçbir sayfa statik üretilemiyor | Giriş durumunun her sayfada görünmesi gereken bir uygulamada kaçınılmaz. Ölçüm yapılmadan optimizasyon yapılmıyor (CLAUDE.md §5.10) | Adım 18 performans ölçümü darboğaz gösterirse |
| 28 | **Başarısız giriş denemesi denetim kaydına yazılmıyor** — yalnızca başarılı giriş ve çıkış yazılıyor | Başarısız denemeler hız sınırı sayacında zaten tutuluyor ve standart bunu ayrıca istemiyor. Her başarısız denemeyi yazmak, hesap sayımı yapan bir saldırganın denetim tablosunu şişirmesine de yol açardı | Güvenlik izleme (adım 18) gerçek bir ihtiyaç gösterirse |
| 29 | **Şifre sıfırlamada iki dar zamanlama/hata kanalı gerçek hesaba özel** | Kod isteme ucu yanıtı sabit 2 saniyeye dolduruluyor, ama (a) e-posta sağlayıcısı çökerse yalnızca gerçek hesabı olan yol 503 alır, (b) sağlayıcı 2 saniyeden yavaş cevap verirse o yanıt tabanı aşar. İkisini de gizlemek, kullanıcıya hiç gelmeyecek bir kodu beklettirmek olurdu (ADR-004'ün "kapı açık bırakılmaz" tercihiyle aynı). Saldırganın bunu kullanabilmesi için sağlayıcının o an bozuk olması gerekiyor | Gönderim kuyruğa alınırsa (yanıt gönderimi beklemez) ikisi de kapanır. ⛔ **Adım 16'da ÖDENMEDİ:** planlı görevler günde bir çalışıyor (borç #3), yani bir kuyruk kodun gönderimi 24 saate kadar geciktirmesi demekti — doğrulama kodu 5 dakikada geçersiz oluyor (`OTP_TTL_MS`). Kuyruk ancak sık çalışan bir işleyiciyle anlamlı | Ücretli plana geçilir ve sık cron mümkün olursa (borç #3) |
| 30 | **Şifre sıfırlamada gönderim bütçesi numaraya bağlı, hedefe DEĞİL** | "Aynı hedefe 3 kod / 15 dakika" kuralı kayıtsız numarada işlemezdi (gönderilecek hedef yok) ve yalnızca gerçek hesapta tetiklenip hesabın varlığını ele verirdi. Bütçe bu yüzden kimlik numarasının özetine bağlandı. Sonuç: aynı e-postaya başka bir akıştan (yeni kayıt) kod gitmişse, gerçek hesap sıfırlamada bir adım erken 429 alabilir | Ödenmesi gerekmiyor — bilinçli takas |
| 31 | **Her yeni dalın preview adresi İKİ panele elle ekleniyor**: Turnstile hostname listesi **ve** Google OAuth "Authorized redirect URIs" listesi (adım 4c'den itibaren) | Turnstile site anahtarı alan adına kilitli ve `*.vercel.app` joker kabul edilmiyor (`vercel.app` bize ait değil, üstünde binlerce yabancı site var). Vercel her dala ayrı preview adresi verdiği için her roadmap adımında panele bir satır ekleniyor; unutulursa bot kutusu preview'da hiç çizilmiyor (`Error: 110200`) ve kodda hata aranıyor — 2026-08-02'de bu iki kez oldu. Google tarafında aynı eksiklik `redirect_uri_mismatch` veriyor. **Aynı alan adı borç #25'i de kapatır** (Resend doğrulanmış alan adı), yani tek alım iki borcu birden öder | Kendi alan adı satın alınıp sabit bir `preview.<alanadi>` tanımlanınca — bir kez eklenir, bir daha uğraşılmaz |
| ~~32~~ | ~~**Google ile giren kullanıcı KPS doğrulamasını henüz yapamıyor** — gidecek sayfası yok~~ | **ÖDENDİ (2026-08-10, adım 15c-2):** `/kimlik-dogrulama` ekranı geldi. Erişim uyarısındaki bağlantı `/kayit` yerine bu ekrana gidiyor ve doğrulama bitince kullanıcı geldiği hizmete dönüyor. Kimlik numarası şifreli + özetli + maskeli yazılıyor, ad soyad KPS'ten geliyor, `isStaff` yalnızca sunucuda hesaplanıyor, işlem denetim kaydına düşüyor | — |
| ~~33~~ | ~~**Profilden Google bağlantısı ekleme/kaldırma yok** (PRD §5.0 bunu istiyor)~~ | **ÖDENDİ (2026-08-10, adım 15c-1):** `/hesabim` → "Giriş yöntemleri" kartı geldi. Bağlama şifre istiyor, son giriş yöntemi kaldırılamıyor (koruma kırmızıya döndürülerek ölçüldü), aynı Google hesabı iki kullanıcıya bağlanamıyor ve iki işlem de denetim kaydına düşüyor | — |
| 34 | **Google OAuth uygulaması "Testing" modunda** — yalnızca Google Console'daki test kullanıcıları giriş yapabilir | Canlıya açmak *Publish app* gerektiriyor; hassas olmayan kapsamlarla Google incelemesi gerekmediği **doğrulanmadı**. Portföy/gösterim için test kullanıcısı yeterli | Gerçek kullanıma açılırken |
| ~~35~~ | ~~**Kimlik doğrulama ekranlarının tipografisi yeni ölçeğe taşınmadı**~~ | **ÖDENDİ (adım 15):** `/giris`, `/kayit`, `/hesabim`, `/sifremi-unuttum` ve bu ekranların paylaştığı bileşenlerde (`StepHeader`, `IdentitySummary`, `StaffOnlyService`, `VerificationPanels`) gövde metni `text-sm` → `text-base` (16px). Etiket, adım sayacı ve yardım metni gibi İKİNCİL metinler 14px kaldı — standart "gövde metni" için 16px istiyor, her metin için değil | — |
| 36 | **Sekme simgesindeki renk token'dan değil elle yazılıyor** — `src/app/icon.svg` içinde `#1e4b8d` sabit | Favicon tarayıcı arayüzünde çizilir, sayfanın CSS değişkenlerine erişemez. Tek çözüm derleme sırasında SVG üretmek olurdu; tek bir renk için derleme adımı eklemek maliyeti faydasından büyük | Marka rengi değişirse elle güncellenecek — dosyanın içinde bu not yazılı |
| ~~39~~ | ~~**Restoran teslimat ücreti SIFIR** — market için 59 TL / 750 TL eşiği tanımlı, restoran için hiçbir doküman ücret vermiyor~~ | **ÖDENDİ (2026-08-07):** proje sahibi değerleri belirledi — **49,90 TL, 400 TL üzeri ücretsiz**. Market ile aynı desen, restorana özel sayılar; eşik düşük çünkü bir öğün siparişi bir market alışverişinden küçük. Değerler `fake-data-guide.md`'ye de yazıldı. Ücret hesabı artık modül başına bir KURAL TABLOSUNDAN okunuyor (`cart-pricing.ts`), iki ayrı `if` dalından değil | — |
| ~~40~~ | ~~**Etkinlik bileti sepette KOLTUĞA değil ETKİNLİĞE bağlı**~~ | **ÖDENDİ (2026-08-08, adım 11):** sepet satırı artık `seat_reservations` kaydını gösteriyor; bir satır = bir koltuk, adet 1'de sabit ve satır koltuğun tam adresiyle görünüyor. Bilet sepete genel sepet ucundan giremiyor — `addCartItemSchema` `event` türünü reddediyor, kimliği sunucu üretiyor | — |
| ~~41~~ | ~~**Kayıtlı kart SİLİNEMİYOR**~~ | **ÖDENDİ (adım 15):** `/hesabim/kartlarim` ekranı geldi. Silme YUMUŞAK (`deleted_at`) — `payments` ve `memberships` karta `Restrict` ile bağlı, mali kayıt kaybolamaz. Üyeliğe bağlı kart kaldırılırken kullanıcı uyarılıyor ama engellenmiyor | — |
| ~~42~~ | ~~**Arama AKSAN KÖRÜ DEĞİL** — "sivi" veya "kagit" yazan kullanıcı ürünü bulamıyor~~ | **ÖDENDİ (2026-08-06):** `unaccent` eklentisi migration ile açıldı; sorgu ve ürün adı aynı sadeleştirmeden geçiyor. Tek çözüm iki sorunu birden kapattı — aksan **ve** büyük harf. Tarayıcıda doğrulandı: `kagit` · `KAGIT` · `KAĞIT` · `kağıt` dördü de "Kağıt Havlu"yu buluyor; `camasir` iki çamaşır ürününü, `yogurt` "Süzme Yoğurt"u getiriyor. Index bilerek eklenmedi (45 satır; ölçümsüz optimizasyon yok) | — |
| 44 | **Aramada yazım hatası affedilmiyor** — "kagıt havulu" veya "kunefe" yerine "kunefee" yazan hiçbir şey bulamaz (market ve restoran, ikisi de) | Yazım hatası toleransı, eş anlamlı sözlüğü ve alaka sıralaması ayrı bir arama motoru işi (Meilisearch, Typesense, Elasticsearch): ek servis, ek hesap, veri senkronizasyonu ve bu proje ölçeğinde karşılığı olmayan bir işletme yükü. 45 ürünlük katalogda kullanıcı zaten kategoriye tıklayarak da bulabiliyor | Katalog gerçek ölçeğe çıkarsa (binlerce ürün) — ADR gerektirir |
| 43 | **Katalog listelerinde sayfalama yok** — market (45 ürün) ve restoran menüsü (31 kalem) tek sayfada, `take: 200` üst sınırıyla | Katalog 45 ürün, menü 31 kalem; kategoriye süzülünce ~10. Sayfalama bugün çözdüğünden fazla karmaşıklık getirirdi (YAGNI) ve her sayfa geçişi ek sorgu demekti. Üst sınır yine de konuldu ki tablo büyürse sayfa boğulmasın | Ürün sayısı 200'e yaklaşınca — o noktada sayfalama VEYA sonsuz kaydırma kararı ADR gerektirir |
| 47 | **Sipariş durumları GERÇEK BİR SÜRECİ yansıtmıyor** — sabit dakika eşiklerinden hesaplanıyor (restoran 10/25/45, market 20/90/240) | Yönetici paneli yok (borç #4) ve sık cron yok (borç #3); durumu ilerletecek ne bir insan ne bir süreç var. Sabit eşik, en azından kullanıcıya tutarlı ve test edilebilir bir akış gösteriyor. Eşikler tek bir kural tablosunda (`ORDER_TIMELINE_RULES`) ve proje sahibinin onayına açık | Faz 2'de yönetici paneli gelirse — o zaman durum gerçek bir olaydan gelir |
| 48 | **Bildirim yalnızca UYGULAMA İÇİ ve ancak kullanıcı ekrana bakınca oluşuyor** — e-posta veya push yok | ADR-013'ün kabul edilen bedeli: bildirimi yazacak bir zamanlayıcı yok, bu yüzden okuma anında yazılıyor. Uygulama içi bildirimde pratik etkisi yok (görmek için zaten girmek gerekiyor). Dışarı çıkan bir kanal eklenirse bu yaklaşım YETMEZ | Gerçek bir bildirim kanalı (e-posta/push) istenirse — kuyruk veya zamanlayıcı gerektirir, ADR-013 gözden geçirilir |
| 49 | **Sipariş listesinde sayfalama yok** — `take: 100` üst sınırıyla tek sayfa | Bugün hiçbir hesapta 100 sipariş yok ve sayfalama her geçişte ek sorgu demek (borç #43 ile aynı gerekçe). Üst sınır yine de konuldu ki tablo büyürse sayfa boğulmasın | Gerçek bir hesap 100 siparişe yaklaşırsa |
| 50 | **Sipariş durumunun zamanla İLERLEDİĞİ canlıda ELLE DOĞRULANMADI** — "20 dakika sonra `Hazırlanıyor`a geçer ve iptal düğmesi kaybolur" davranışı yalnızca otomatik testlerde görüldü | Doğrulama için gerçek zamanda 20 dakika (markette) beklemek gerekiyor ve proje sahibi ekran başında o kadar bekleyemedi. **Kanıt eksik değil, sadece elle görülmedi:** aynı kod yolu `tests/db/order-cancellation.test.ts` içinde sahte saat ileri sarılarak koşuyor ve dört aşamanın dördü de doğrulanıyor (`Alındı → Hazırlanıyor → Yola çıktı → Teslim edildi`), ayrıca eşik geçildikten sonra gelen iptal isteğinin 409 döndüğü de kanıtlanıyor. Risk düşük çünkü durumu üreten fonksiyon saf ve zamanı dışarıdan alıyor (ADR-013) | **#62 ve #73 ile BİRLİKTE, tek oturumda** — proje sahibi 2026-08-10'da "hepsini topluca test edeyim" dedi. Sıra ve süreler `sonraki-adim-prompt.md` → "TOPLU ELLE TEST" bölümünde: restoran siparişi eşiği 10 dakika, market 20. Daha hızlısı isteniyorsa restoran siparişi kullanılır — orada eşik 10 dakika |
| 45 | **Restoran hazırlık süresi sipariş kaydına YAZILMIYOR** — 30-45 dakika yalnızca ekranda gösteriliyor, `orders` tablosunda karşılığı yok. **Adım 10'da ÖDENMEDİ:** sipariş takibi bir hazırlık süresi üretiyor ama bu da bir TAHMİN (ADR-013 sabit eşikleri), ölçüm değil; kayda yazmak yine karşılığı olmayan bir söz olurdu | Mutfağın gerçek doluluğunu bilen bir sistem yok; kayda yazılan bir süre "söz verilen saat" gibi görünür ve tutmadığında haklı bir şikâyet konusu olurdu. Bugün gösterilen sabit bir tahmin ve ekranda da tahmin olduğu yazıyor | Sipariş takibi (adım 10) gerçek bir hazırlanma süresi üretirse — o zaman tahmin değil ölçüm olur |
| 46 | **Mutfak notu SEPET satırında yaşıyor, sipariş kalemine kopyalanmıyor** — ödeme sonrası `order_items` tablosunda not alanı yok | `order_items` şeması adım 3'te not alanı olmadan kuruldu ve şema bu adımda bilerek değiştirilmedi (adım 9 kapsamı "şema DEĞİŞMİYOR"). Bugün not ödeme anına kadar taşınıyor ve ekranda görünüyor; kaybolduğu tek an sipariş oluşturulduktan sonrası. Gerçek bir mutfak entegrasyonu olmadığı için pratik etkisi yok. **Adım 10'da ÖDENMEDİ:** sipariş ekranı kalemleri adı ve adediyle gösteriyor, notu göstermiyor. Kolonu eklemek bu adımın kapsamını (durum + bildirim + iptal) genişletirdi ve adım 10 zaten şemaya iki ekleme yapıyordu | Notun sipariş geçmişinde görünmesi istenirse — `order_items.note` kolonu geriye uyumlu biçimde eklenir, ödeme akışında sepet satırından kopyalanır |
| 51 | **Koltuk seçmek GİRİŞ GEREKTİRİYOR** — ziyaretçi etkinlikleri ve salon planını görüyor ama koltuğa basınca giriş ekranına yönleniyor. Market ve restoranda ziyaretçi sepete ekleyebiliyor (PRD §4), bilette ekleyemiyor | Veri modelinin sonucu, tercih değil: `seat_reservations.user_id` ZORUNLU, yani ziyaretçi adına kilit yazılamaz. Nullable yapıp ziyaretçi kimliğine bağlamak, 10 dakika boyunca koltuğu kimliksiz birine ayırmak ve "bu kilit kimin" sorusunu ödeme anına ertelemek olurdu. Ekran girişe kapatılmadı — kullanıcı salonun doluluğunu giriş yapmadan da görebiliyor | Ziyaretçi bileti gerçek bir ihtiyaç olursa — `user_id` nullable + `anonymous_id` kolonu gerektirir, yani şema değişikliği ve ADR |
| 52 | **Salon planındaki koltuk dokunma hedefi 36px** — `07-ui-design-system.md` en az 44px istiyor | 8 koltukluk bir sırada her koltuğa 44px vermek 375px'lik ekranda 350px'lik bir sıra demek ve sıra etiketiyle birlikte taşıyor. 36px + aralarındaki boşluk parmak için ayırt edilebilir kalıyor; yanlış koltuğa basmak da fark ediliyor çünkü seçilen koltuk aşağıdaki listede TAM ADRESİYLE yazıyor. Bilinçli ve gerekçesi kodda yazılı bir sapma | Salon planı yeniden tasarlanırsa (yakınlaştırma/kaydırma eklenirse) |
| ~~53~~ | ~~**Süresi dolmuş kilitler yalnızca SEPET OKUNDUĞUNDA siliniyor**~~ | **ÖDENDİ (2026-08-10, adım 16):** `cleanup_seat_holds` görevi geldi. ⛔ Silme koşulu `status = 'held'` — SATILMIŞ bilete dokunmuyor; koşul kaldırılıp testin kırmızıya döndüğü görüldü, çünkü kaybolacak şey bir mali kayıt olurdu | — |
| 54 | **Salon planı 192 koltuğu tek seferde çiziyor** — sanallaştırma veya sayfalama yok | Üç salonun üçü de 192 koltuk ve ölçüm yapılmadan optimizasyon yapılmıyor (CLAUDE.md §5.10). Sayfalama bir salon planında zaten anlamsız: kullanıcı salonun tamamını görmek istiyor | Gerçek bir salon binlerce koltuğa çıkarsa — o noktada sanallaştırma ADR gerektirir |
| 20 | **`timeout` simülasyonu 6 sn sunucu zamanı harcıyor** | Çağıran 3 sn'de vazgeçse de sahte servis beklemeye devam ediyor; Vercel'de bu boşa giden fonksiyon süresi demek. Gerçek bir zaman aşımını taklit etmenin daha ucuz bir yolu yok | Ödenmesi gerekmiyor — yalnızca 3 sınır durum numarasında tetikleniyor |
| 37 | **"Aynı branşta aynı gün ikinci randevu" kuralında dar bir yarış aralığı var** — aynı kullanıcı iki sekmeden TAM AYNI ANDA iki farklı saate basarsa ikisi de geçebilir. Kural transaction içinde uygulanıyor ama iki eşzamanlı işlem birbirinin henüz commit edilmemiş randevusunu göremiyor | Veritabanı seviyesinde kapatmanın iki yolu vardı: (a) `data-model.md`'nin reddettiği bir denormalize kolon + unique index eklemek, (b) yalıtım seviyesini `Serializable`'a çıkarmak — o da farklı saatlere yazan meşru isteklerde sahte 409'lar üretebilirdi. **Çift satış riski YOK**: saatin kendisi `DoctorSlot.isBooked` üzerindeki koşullu güncellemeyle korunuyor (PRD kabul kriteri), delinen şey yalnızca kullanıcının kendi kolaylık kuralı. Gerçekleşmesi için aynı kişinin iki sekmede milisaniye farkıyla tıklaması gerekiyor | Gerçek bir şikâyet gelirse veya adım 7'de sepet/ödeme aynı deseni gerektirirse birlikte çözülür |
| 38 | **Tohumlanmış doktor saatleri tohumlama gününden 14 gün sonra tükeniyor** — o tarihten sonra hastane ekranı "boş saat kalmamış" gösterir | Seed `context.today`'den itibaren 14 günlük takvim üretiyor (`prisma/seed/steps/health.ts`). Sabit bir tarihe bağlamak takvimi hemen geçmişe düşürürdü; daha uzun pencere ise tohumlama süresini ve satır sayısını katlardı. Preview ve production 2026-08-01'de tohumlandı, yani saatler ~2026-08-15'te bitiyor. Ekran boş durumu düzgün gösteriyor, çökme yok | **ÖDENDİ (2026-08-10, adım 16):** `extend_doctor_calendar` görevi her gün takvimi bugünden itibaren 14 güne tamamlıyor. Saatler artık iki yerde değil TEK yerde tanımlı (`slot-calendar.ts`) — tohum da oradan okuyor, yoksa 15. günün saatleri ilk 14 günden farklı olurdu. Görev "bir gün ekle" değil "eksikleri tamamla" biçiminde: cron bir hafta hiç çalışmazsa ilk koşu boşluğun tamamını kapatıyor. ⛔ İlk canlı koşuya kadar production'da saatler 2026-08-15'te bitmeye devam ediyor |
| ~~55~~ | ~~**Yenileme tahsilatını çalıştıran planlı görev YOK**~~ | **ÖDENDİ (2026-08-10, adım 16):** `renew_memberships` ve `send_renewal_reminders` görevleri geldi. Tahsilat mantığı KOPYALANMADI — görev adım 12'de yazılan `renewMembershipPeriod()`'u çağırıyor; hatırlatma da ekranın kullandığı `syncMembershipNotifications()`'ı çağırıyor, yani "zamanı geldi mi" kuralı hâlâ tek yerde. İdempotentlik ölçüldü: görev aynı gün iki kez koşturuldu, vadesi gelen dönem için `membership_payments`'ta **tek satır** kaldı (PRD §5.6 kabul kriteri). Anahtar dönemden türetiliyor (`renewal:<üyelik>:<dönem>`), rastgele değil | — |
| 56 | **İptal sırasında erken çıkış farkı çekilemezse üyelik `payment_pending` OLMUYOR** — `cancelled` kalıyor, başarısız deneme `membership_payments`'a yazılıyor ve borç görünür kalıyor. PRD §5.6 bu durumda "ödeme bekliyor" diyor | İptali kullanıcı açıkça istedi ve koşullu güncelleme onu üstlendi; durumu `payment_pending`e çevirmek o iptali silmek olurdu. Ayrıca kullanıcının borcu ödeyebileceği bir ekran bu adımda yok — "ödeme bekliyor" demek, karşılığı olmayan bir eylem beklemek olurdu. Yenileme tahsilatında kural AYNEN uygulanıyor (üyelik devam ediyor, ödenecek bir dönem var) | Borç ödeme ekranı gelirse (adım 15 profil veya adım 16) |
| 57 | **Ders programı ve tesis bilgisi kodda sabit** (`src/features/gym/data/facility.ts`), veritabanında değil | `data-model.md` spor salonu için üç tablo tanımlıyor ve ders programı için tablo YOK; değiştirecek yönetici paneli de yok (borç #4). Salt okunur, hiç yazılmayan bir içeriği tabloya taşımak bugün hiçbir şey kazandırmazdı (YAGNI) | Program yönetilebilir olması istenirse — tablo + ADR gerektirir |
| 58 | **Derse kayıt/kontenjan yok** — program yalnızca görüntüleniyor | PRD §5.6 yalnızca "ders programı görüntülenir" diyor; kayıt ayrı bir tablo, kontenjan yarışı ve iptal kuralı demek | PRD'ye ders kaydı eklenirse |
| ~~59~~ | ~~**Spor salonu üyeliği GERÇEK CİHAZDA denenmedi**~~ | **ÖDENDİ (2026-08-09):** proje sahibi telefonundan baktı — paket kartları ve haftalık ders programı dar ekranda taşmıyor, sayfa yana kaymıyor. Ders programının aşağı doğru uzaması tasarımın kendisi: tablo değil kart ızgarası (telefonda tek, tablette iki, masaüstünde üç sütun) | — |
| 60 | **Ek dosyalar VERİTABANINDA duruyor, nesne deposunda değil** — `00-stack.md` Vercel Blob diyor, bugün `db` sürücüsü çalışıyor (ADR-014) | Panelde açılmış bir Blob store yok ve onu yalnızca proje sahibi açabilir; store açılsa bile local/CI için ikinci bir sürücü zaten gerekiyordu (canlı anahtar local'de kullanılmaz). Sapma tek dosyaya hapsedildi: uygulama `FileStorage` arayüzüne konuşuyor, geçiş tek dosyalık. Proje sahibi 2026-08-09'da "şimdilik veritabanı kalsın" dedi | Blob store açılınca **veya** veritabanı boyutu Neon ücretsiz katmanının (0,5 GB) yarısını geçince |
| 61 | **Destek talebi durum eşikleri BİR VARSAYIM** — 30 dakika sonra `İnceleniyor`, 180 dakika sonra `Çözüldü` | PRD §5.7 durumları sayıyor ama süre vermiyor. Sipariş eşiklerindeki (borç #47) aynı durum: yönetici paneli yok (borç #4), sık cron yok (borç #3), yani durumu ilerletecek gerçek bir süreç yok. Değerler tek yerde (`constants.ts`) ve proje sahibinin onayına açık; ekranda "gerçek bir görevli bakmaz" diye YAZIYOR | Faz 2'de yönetici paneli gelirse |
| 62 | **Destek talebinin zamanla İLERLEDİĞİ canlıda ELLE DOĞRULANMADI** — ilk eşik 30 dakika | Borç #50 ile aynı sebep: gerçek zamanda 30 dakika beklemek gerekiyor. Kanıt eksik değil, sadece elle görülmedi — `tests/db/support-ticket.test.ts` sahte saati ileri sararak üç aşamayı da doğruluyor ve durumu üreten fonksiyon saf (ADR-013) | **#50 ve #73 ile BİRLİKTE, tek oturumda** — talep açılır, 30 dakika sonra `İnceleniyor` olduğu görülür. Adımlar `sonraki-adim-prompt.md` → "TOPLU ELLE TEST" bölümünde |
| ~~63~~ | ~~**Dış veri önbelleğini temizleyen planlı görev YOK**~~ | **ÖDENDİ (2026-08-10, adım 16):** `cleanup_external_cache` görevi geldi. ⚠️ Ölçütü diğer temizliklerden FARKLI ve sebebi ADR-015: burada süresi dolmuş kayıt ölü değil, sağlayıcı çöktüğünde 24 saate kadar "güncellenemiyor" notuyla ekrana çıkabiliyor. Bu yüzden ölçüt `expiresAt` değil `fetchedAt`, pay da `INFO_WIDGET_MAX_STALE_MS`. `expiresAt`'e göre silen bir görev, tam da sağlayıcının çöktüğü gün yedeği silerdi | — |
| 64 | **Haber kaynağı TEK ve yedeksiz** — akış biçim değiştirir, engellenir veya kaybolursa haber kartı hata gösterir | ADR-016: ikinci bir sağlayıcı eklemek iki ayrı ayrıştırma yolu, iki devre kesici ve "hangisi kazanır" kuralı demek. Kart tek başına bozuluyor, sayfa ayakta kalıyor ve 24 saate kadar bayat veri sunuluyor (ADR-015) — yani kesintinin bedeli sınırlı. Akış adresi `constants.ts` içinde tek satır | Akış fiilen bozulursa veya belediyeye ait gerçek bir duyuru kaynağı gerekirse |
| 65 | **CoinGecko'nun anahtarsız ucu production'da 429 yiyebilir ve bu ÖLÇÜLMEDİ** — Vercel'in çıkış IP'leri paylaşımlı | 5 dakikalık önbellek istek sayısını saatte ~12'ye indiriyor ve `429` yeniden denenmiyor (sınırı derinleştirmemek için). Takılırsa kullanıcı boş kart değil bayat fiyat görüyor. Gerçek kullanıcı sayısı 0 olduğu için bugün ölçülemez de | Canlıda trafik oluşunca — ya da kripto bölümü kalıcı olarak bayat görünürse |
| 66 | **Önbellek boşken eşzamanlı istekler aynı dış çağrıyı birden çok kez yapabilir** ("thundering herd") — süre dolduğu anda gelen N ziyaretçi N çağrı tetikler | Kilit koymak, kilidi de sunucusuz ortamda paylaşılan bir yere (Postgres) yazmak demek; kazancı bugün ölçülemez çünkü gerçek kullanıcı sayısı 0. Zarar sınırlı: devre kesici üst sınırı koyuyor, `429` yeniden denenmiyor ve fazladan çağrılar aynı değeri yazdığı için veri bozulmuyor | Trafik oluşup sağlayıcı limitleri fiilen zorlanırsa — ya da adım 16'da önbelleği planlı görev doldurmaya başlarsa kendiliğinden |
| 67 | **Adres düzenlemesi GEÇMİŞ SİPARİŞİN teslimat adresini de değiştirir** — sipariş anında adresin kopyası alınmıyor, `orders.delivery_address_id` canlı satırı gösteriyor | Kopya alan bir kolon eklemek şema değişikliği ve geçmiş satırların doldurulması demek; bugün kullanıcıya görünür etkisi YOK çünkü sipariş ekranları teslimat adresini hiç çizmiyor. Adresi düzeltebilmenin kazancı, görünmeyen bu tutarsızlığın bedelinden büyük | Sipariş detayında teslimat adresi gösterilmeye başlanırsa — o an sipariş satırına adres anlık kopyası eklenmeli |
| 68 | **Adres üst sınırı (20) YARIŞA AÇIK** — "önce say, sınırın altındaysa yaz" iki adım; aynı anda gelen iki istek ikisinde de sınırın altını görebilir | Koltuk kilidi veya stok düşümünün aksine yarışın bedeli bir kullanıcının kendi hesabında 20 yerine 21 adres görmesi: para, yer veya başkasının hakkı kaybolmuyor. Veritabanı seviyesinde kısıt (sayaç kolonu + koşullu artırma) bu bedele değmez | Sınır gerçek bir kaynağı korumaya başlarsa (örn. adres başına ücret) |
| 69 | **`GET /api/addresses` ve `GET /api/saved-cards` uçları YOK** — listeler yalnızca sunucu bileşeninden okunuyor | Kimsenin çağırmadığı uç bedava değil: bakılması, test edilmesi ve korunması gereken bir yüzey (CLAUDE.md §5.2 YAGNI). Bugün hiçbir istemci HTTP üzerinden liste istemiyor | Mobil uygulama (adım 19) — kendi testiyle birlikte eklenir |
| 70 | **Profil merkezinde kayıt SAYACI yok** — kartlar "3 sipariş" gibi bir sayı göstermiyor, yalnızca bağlantı veriyor | Sipariş, destek talebi ve üyelik durumları OKUMA ANINDA türetiliyor (ADR-013). Ham sayı aktifle teslim edilmişi aynı torbaya koyardı; doğru sayı ise profil sayfasının beş ayrı modülün türetme mantığını çalıştırması demek olurdu (CLAUDE.md §7: aynı anda tek modül). Adres ve kart sayaçları VAR — onların durumu türetilmiyor | Modüller "aktif kayıt sayısı" sorusuna kendi katmanlarında cevap verecek hâle gelirse |
| 71 | **`sitemap.xml` hâlâ EKSİK** — `/hakkimizda` ve dört yasal sayfa eklendi (adım 17), ama `/market`, `/restoran`, `/etkinlikler`, `/spor-salonu` hâlâ yok | **KISMEN ÖDENDİ (2026-08-10, adım 17):** yasal sayfalar `LEGAL_PAGES` kataloğundan üretiliyor, yani yeni bir belge eklendiğinde site haritası kendiliğinden büyüyor. Kalan dört hizmet sayfası için aynı deseni kurmak, o modüllerin her birine dokunmayı gerektirirdi (CLAUDE.md §7: aynı anda tek modül). Site `noindex` olmayan tek ortamda canlı ve gerçek kullanıcı sayısı 0 | Adım 18 (üretime hazırlık) — kalan herkese açık sayfalar tek bir listeden eklenir |
| 72 | **Personel rehberi sayfalama YAPMIYOR** — 100 kayıt tek sayfada çiziliyor (sorgu 200 satırla sınırlı) | Ölçüm yapmadan sayfalama eklemek erken optimizasyon olurdu (CLAUDE.md §5.10): 100 kart mobilde bile sorunsuz çiziliyor ve süzgeçler listeyi zaten daraltıyor. Sınır yine de var — `take` olmadan büyüyen bir tablo sessizce sayfayı boğardı | Rehber 200 kaydı aşarsa — o an sayfalama ya da "daha fazla göster" eklenmeli |
| 73 | **İki ekranın TELEFON TESTİ yapılmadı** — adım 15 (profil: `/hesabim`, adreslerim, kartlarım) ve adım 15b (`/hakkimizda`). İkisi de canlıda, ikisi de yalnızca masaüstü tarayıcı ve Playwright `mobile-375` projesiyle doğrulandı | Proje sahibi ikisini de bilinçli olarak erteledi (2026-08-09 ve 2026-08-10): "şimdilik merge et, telefonla sonra bakarım". Otomatik testler ve 375px ölçümü yeşil, yani bilinen bir arıza yok — eksik olan yalnızca gerçek cihazda dokunma deneyimi | **#50 ve #62 ile BİRLİKTE, tek oturumda** — proje sahibi 2026-08-10'da "hepsini topluca test edeyim" dedi. Tıklama listesi `sonraki-adim-prompt.md` → "TOPLU ELLE TEST" bölümünde sıralı ve süre sayacıyla birlikte yazılı |
| 74 | **Google bağlantısı kaldırılırken ŞİFRE SORULMUYOR** — yalnızca satır içi onay isteniyor (bağlamada şifre soruluyor) | Kaldırma bir YETKİ ARTIRIMI değil, azaltımı: çalınmış bir oturumla yapılabilecek en kötü şey kurbanın Google girişini kapatmak ve kurbanın şifresi zaten duruyor (son giriş yöntemi kuralı kilitlenmeyi engelliyor). Şifreyi burada da sormak, asıl riski olmayan bir adıma sürtünme eklerdi | Hesap ele geçirme senaryosunda "saldırgan kurbanın erişimini kısıtlıyor" gerçek bir şikâyete dönüşürse |
| 75 | **Bağlama akışının GOOGLE'A GİDEN adımı otomatik test edilmiyor** — testler bizim ucumuzun 303 + `accounts.google.com` üretmesinde duruyor | Akışın ortasında gerçek Google ekranı var; onu otomatikleştirmek ya gerçek bir Google hesabıyla oturum açmak ya da sağlayıcı adresini dışarıdan yönlendirilebilir yapmak demekti. İkincisi üretim koduna test için bir kapı açardı (adım 4c'de aynı karar verilmişti). Bağlantının veritabanına doğru yazıldığı gerçek PostgreSQL'e karşı kanıtlanıyor | Sağlayıcı taklidi (mock OIDC sunucusu) kurulursa — adım 18'de değerlendirilir |
| 76 | **KİMLİK KANITININ GÜCÜ: doğrulama için T.C. kimlik numarası + doğum yılı YETİYOR** — ikisi de Türkiye'de gizli bilgi değil. Kurbanın numarasını bilen biri kendi hesabını onun adıyla doğrulatabilir; kurban personel rehberindeyse ve henüz hesabı yoksa `isStaff` de kazanır. Üstelik bir numara yalnızca bir hesaba bağlandığı için gerçek kişi kendi kimliğini artık doğrulatamaz ve **bağlantıyı çözecek hiçbir akış yok** | Aynı kanıt gücü KAYIT akışında da geçerli (PRD §5.0): oradaki OTP saldırganın KENDİ e-postasını doğruluyor, kurbanınkini değil. Yani bu adım yeni bir açık AÇMADI, mevcut tasarım kararını yeni bir kapıya taşıdı. Sahte KPS ile çalışan bir portföy projesinde gerçek kimlik kanıtı (e-Devlet, banka doğrulaması) yok | **KARAR VERİLDİ (2026-08-10) → ADR-017.** Proje sahibi "gerçek veriyle çalışacağız, best practice neyse o" dedi. Karar: kimlik kanıtı **adaptör sınırına** alındı, bugünkü bilgi temelli kanıt **geçici** işaretlendi, gerçek sağlayıcıya (e-Devlet) geçiş tek dosyalık iş olarak tanımlandı. OTP seçeneği REDDEDİLDİ (kod saldırganın kendi kanalına gidiyor — güvenlik tiyatrosu), ek KPS alanı sormak da REDDEDİLDİ (kanıtın sınıfını değiştirmiyor). Bugün kod DEĞİŞMEDİ; doğan üç iş adım **17b** (kimlik çözme akışı), adım **17c** (yetki/kimlik ayrımı) ve **faz 2** (gerçek sağlayıcı) olarak roadmap'e girdi. ✅ **17b BİTTİ (2026-08-10):** çözme akışı geldi, numara serbest kalıyor ve elle veritabanı müdahalesi artık gerekmiyor — ama zaafın kendisi kapanmadı (borç #90). ✅ **17c BİTTİ (2026-08-11):** kimlik doğrulaması ARTIK YETKİ VERMİYOR; `matchStaffMember` silindi ve `isStaff` iki yazma yolundan da (kayıt + kimlik bağlama) çıkarıldı. Yani bu borcun EN AĞIR bacağı — "kurbanın numarasını bilen onun personel yetkisini de kazanır" — KAPANDI. Kalan zaaf yalnızca kimlik tarafında: saldırgan hâlâ kurbanın adıyla doğrulama yapabilir, ama artık bununla hiçbir yetki kazanamaz. **Kalan: faz 2 (gerçek kimlik sağlayıcısı).** Borç o iş bitince kapanır — ve o güne kadar **gerçek kişisel veriyle çalıştırma kapısı KAPALI** |
| 77 | **Denetim kaydı ile kimlik bağlama AYNI transaction'da değil** — bağlama başarılı olup denetim kaydı yazılamazsa kullanıcı 500 görüyor, tekrar denediğinde "zaten doğrulanmış" diyor ve yetki değişikliğinin kaydı kayboluyor | Proje genelinde aynı desen kullanılıyor (`google-link`, profil adres/kart yazmaları): `recordAuditLog` global istemciyle çalışıyor, transaction istemcisi almıyor. Yalnızca bu akışta değiştirmek deseni bölerdi ve `lib/audit.ts`'i (paylaşılan katman) bu adımda değiştirmek kapsamı genişletirdi | `recordAuditLog` transaction istemcisi kabul edecek biçimde genişletilince — tek seferde TÜM yazma akışları için |
| ~~78~~ | ~~**Hız sınırının IP bacağı `x-forwarded-for` ile atlatılabilir olabilir**~~ | ⛔ **KAPANDI (2026-08-12, adım 18d) ve BU KAYDIN ENDİŞESİ DE ÖNERDİĞİ ÇÖZÜM DE YANLIŞMIŞ.** Kayıt "doğrulanmadan karar verilmemeli" diyordu; doğrulandı. Vercel `docs/headers/request-headers`: *"we currently **overwrite** the X-Forwarded-For header and **do not forward external IPs**. This restriction is in place to **prevent IP spoofing**."* Yani istemci bu başlığı sahteleyemiyor — atlatma senaryosu mümkün değil. Dahası aynı belge `x-real-ip` için *"identical to the x-forwarded-for header"* diyor, yani kaydın önerdiği çözüm (`x-real-ip`'i önce oku) **hiçbir şeyi değiştirmezdi**. Fiilen yapılan: sıra `x-vercel-forwarded-for` → `x-forwarded-for` → `x-real-ip` oldu. Gerekçe bugünü değil YARINI korumak — uygulamanın önüne bir gün vekil konursa `x-forwarded-for` o vekilin değeri olur, `x-vercel-forwarded-for` platformunki kalır. 4 test sırayı kilitliyor, mutasyonla kanıtlandı. ⭐ Ders: bir borç kaydının ÖNERDİĞİ ÇÖZÜM de bir iddiadır | — |
| ~~79~~ | ~~**500 hatalarında sunucu log'una kişisel veri düşebilir**~~ — `fail()` beklenmeyen hatanın `cause`'unu olduğu gibi yazıyor; Prisma doğrulama hatası argüman nesnesinin tamamını (ad soyad, doğum tarihi, maskeli numara) hata metnine koyuyor | Ham kimlik numarası girmiyor (şifreli/özetli/maskeli haller giriyor) ve bu yol yalnızca beklenmeyen bir hatada çalışıyor. Yine de 05-auth-security.md'nin "log'a yazılmaz" kuralını zorluyor **ÖDENDİ (2026-08-11, adım 18a).** `fail()` artık `logger.error`'a yazıyor ve her bağlam `redact()`ten geçiyor. ⭐ **Asıl iş alan adına bakan süzgeç DEĞİL, DEĞERİN BİÇİMİNE bakan süzgeç:** Prisma argümanları hata metninin İÇİNE düz yazı koyduğu için ad soyad ve kimlik numarası bir alan adının arkasında değil, cümlenin ortasında duruyordu. Aynı süzgeç Sentry'nin `beforeSend`ine de bağlandı, yani veri üçüncü servise de gitmiyor. Cron'un iki log satırı (adım 16'nın eklediği ikinci bacak dahil) aynı yoldan geçiyor. **Koruma mutasyonla ölçüldü:** biçim-tabanlı süzme kapatılınca 9 test kırmızıya döndü |
| 81 | **Temizlik görevleri SAYFALAMA YAPMIYOR** — her görev tek `DELETE` ile süresi geçmiş TÜM satırları siliyor, üst sınır yok | Sayfalanmış silme (önce kimlikleri seç, sonra sil) yazıldı ve ATILDI: bu tabloların hiçbiri bugün dört haneli satır sayısına ulaşmıyor ve ölçüm yapmadan optimizasyon yapılmıyor (CLAUDE.md §5.10). Görev bir gün fonksiyon süresini aşarsa hiçbir şey bozulmaz — silme idempotent, ertesi gün kaldığı yerden devam ediyor ve kullanıcı bu arada yanlış sonuç görmüyor (ADR-007) | Bir tablo ölçülebilir biçimde büyürse — o an `DELETE ... LIMIT` deseni eklenir |
| 82 | **`payment_pending` üyeliklerde tahsilat YENİDEN DENENMİYOR** — kartı reddedilen üyelik 3 günlük süre boyunca her gün yeniden denenmiyor, süre dolunca sona ermiş sayılıyor | Her gün yeniden denemek kullanıcının kartını arka arkaya zorlamak olurdu ve gerçek ödeme sağlayıcılarında art arda ret, kartın işaretlenmesine yol açabilir. PRD §5.6 yeniden deneme İSTEMİYOR; kullanıcı bu arada bildirim alıyor ve kartını güncelleyebiliyor | Kullanıcıya "borcumu şimdi öde" ekranı gelirse (borç #56 ile birlikte) — o zaman yeniden deneme kullanıcının kararı olur |
| 83 | **Cron'un ÇALIŞTIĞI canlıda henüz görülmedi** — kod local'de uçtan uca koşturuldu (9 görev, 0 hata, denetim kayıtları yazıldı) ama production'da ilk tetikleme deploy'dan sonraki gece | Vercel ücretsiz planda cron günde bir çalışıyor ve saati garanti etmiyor (belirtilen saatin içinde herhangi bir dakika); beklemeden görmenin yolu yok. Elle tetiklemek mümkün ama `CRON_SECRET` ile — o da ayrı bir doğrulama | Deploy'dan sonraki ilk gece — `/api/cron/daily` çağrısı Vercel panelinde **Settings → Cron Jobs → View Logs** altında görünür. Doktor takviminin production'da 14 güne uzadığı da o gün doğrulanır |
| ~~80~~ | ~~**Google ile açılan hesabın TELEFONU boş kalıyor**~~ | **ÖDENDİ (2026-08-10, adım 17b):** `/hesabim/verilerim` → "Cep telefonum" formu geldi. ⚠️ Numara **doğrulanmamış** yazılıyor (`phoneVerifiedAt = null`) ve ekranda "Doğrulanmadı" rozeti görünüyor — OTP adımı BİLEREK eklenmedi: bu projede telefon doğrulaması simüle (borç #1), kod telefona değil kullanıcının KENDİ e-postasına gidiyor, yani numaranın ona ait olduğunu kanıtlamıyor. ADR-017 aynı gerekçeyle ("güvenlik tiyatrosu") kimlik doğrulamasına OTP eklemeyi reddetmişti. Kanıtı üretmemek ile üretmiş gibi göstermek arasında tercih yapıldı ve ekranda YOKLUĞU yazıldı | — |
| ~~84~~ | ~~**Çerez bandı uygulamanın TAMAMINI istek anında çizilir hâle getirdi**~~ | ⛔ **KAPANDI (2026-08-11, adım 18c) ve BU KAYDIN SEBEBİ YANLIŞMIŞ.** Mutasyonla ölçüldü: bandı tek başına kaldırmak HİÇBİR sayfayı statikleştirmiyor, `SiteHeader`'ı tek başına kaldırmak da öyle. Sayfaların hepsi zaten dinamikti — başlık oturum çerezini **adım 4a'dan** beri (commit `49f965c`, adım 17'den çok önce) okuyor. Bant ikinci, eşit bir sebep; tek başına hiçbir şeyi bozmadı. İkisi birden kaldırıldığında yalnızca **6 sayfa** statikleşiyor (`/gizlilik` · `/iletisim` · `/kullanim-sartlari` · `/hesap-silindi` · `/kayit/tamamlandi` · `/_not-found`). **Bedel artık ölçüldü ve kabul edilebilir:** o sayfaların LCP'si canlıda 720 ms, hedef 2500 ms — §5.10 gereği ölçüm hedefin altındaysa optimizasyon yapılmaz. ⭐ Ders: bir borç kaydındaki SEBEP de bir iddiadır ve doğrulanmadan devralınmaz | — |
| 85 | **Kayıt akışında ONAY KUTUSU yok** — kullanıcı "kayıt olarak Şartlar'ı ve Aydınlatma Metni'ni kabul etmiş olursunuz" cümlesini görüyor, hesap açılırken iki rıza kaydı yazılıyor; ama işaretlenmiş bir kutu kanıtı yok | Zorunlu bir onay kutusu kayıt şemasını, servisini ve dört ayrı testi değiştirmeyi gerektirirdi — adım 17 çerez rızası adımıydı, kayıt akışı adım 4b'nin modülü (CLAUDE.md §7). Metinler ziyaretçiye açık, bağlantılar kayıt düğmesinin dibinde ve kayıt zaman damgalı. Yine de "gördü" ile "işaretledi" arasındaki fark gerçek bir kanıt farkıdır | ⛔ **Adım 17b'de ÖDENMEDİ ve bu bilinçli:** 17b kayıt akışına HİÇ dokunmadı (kendi ekranı, kendi uçları, kendi katmanı). Kutuyu eklemek kayıt şemasını, servisini ve dört ayrı testi değiştirmek demekti — CLAUDE.md §7: aynı anda tek modül. Kayıt akışına dokunulacak İLK adımda çevrilecek |
| 87 | **SAKLAMA SÜRESİ DOLAN KAYDI İMHA EDEN HİÇBİR ŞEY YOK** — "mali kayıtlar 10 yıl saklanır" diyoruz ama 11. yılda onları silecek veya gerçekten anonim hale getirecek bir görev yazılmadı. Aynı şey `SupportTicket` (3 yıl), `Appointment`/`SeatReservation` (3 yıl), `Notification` (1 yıl) ve `KpsQueryLog` (90 gün) için de geçerli | Proje 2026-07-30'da başladı; bugün hiçbir kayıt saklama süresinin yarısına bile gelmedi, yani yazılacak kod bugün hiçbir satıra dokunmazdı. Ayrıca gerçek kullanıcı sayısı 0. **Ama bu bir hatırlanacak iş değil, YAZILI bir borç:** "10 yıl saklıyoruz" deyip 11. yılda hâlâ tutmak, KVKK Yönetmeliği m.11'in ihlalidir ve süre dolduğunda kimse hatırlamaz. Günlük planlı görev (adım 16) altyapısı zaten var — eklenecek şey yeni bir görev kolu | Gerçek kişisel veriyle çalıştırma kapısı açılmadan önce (ADR-017 üçlüsüyle birlikte). ⚠️ Periyodik imha aralığı Yönetmelik m.11/2 gereği **altı ayı geçemez**; günlük cron bunu fazlasıyla karşılıyor |
| 88 | **Veri indirme KUYRUĞA ALINMIYOR, anında üretiliyor** — PRD §5.11 "hazırlama uzun sürerse istek kuyruğa alınır ve hazır olduğunda bildirim düşer" diyor | Ücretsiz planda planlı görev günde bir çalışıyor (borç #3), yani bir kuyruk indirmeyi **24 saate kadar geciktirmek** demekti — kullanıcı için bugünkünden kesinlikle kötü. Tek bir kullanıcının kayıtları dokuz tabloda toplam birkaç yüz satır ve dokuz sorgu paralel koşuyor. Bugünkü hacimde anında üretmek hem doğru hem hızlı | Gerçek trafik oluşup indirme süresi ölçülebilir hâle gelirse — ya da sık çalışan cron mümkün olursa (borç #3) |
| 89 | **Şifresi OLMAYAN hesapta silme ve kimlik çözme için ikinci kanıt YOK** — Google ile açılmış hesapta tek kanıt oturumun kendisi | Zorunlu kılmanın tek yolu Google'a yeniden gönderip dönüşü beklemekti (yeni bir OAuth modu, yeni callback dalı). Alternatif "şifre belirle" demekti — kullanıcıyı hesabını silmek için şifre kurmaya zorlamak. **KVKK m.11 silme hakkını bir teknik ayrıntıya bağlamaz:** o hesapların hiç silinememesi, ikinci kanıt eksikliğinden daha ağır bir sorun olurdu. Şifresi olan hesaplarda kapı çalışıyor | ⚠️ **PROJE SAHİBİNİN KARARI BEKLENİYOR** (adım 18d raporu §5.2): (a) "yeniden kimlik doğrula" OAuth modu ekle (~1 oturumluk iş), (b) bugünkü hâliyle bırak |
| 90 | **Kimlik çözme, ADR-017'deki asıl zaafı KAPATMIYOR** — bağı yalnızca onu KURAN hesap çözebiliyor. Kurbanın numarasını kendi hesabına bağlamış bir saldırgan gönüllü çözmezse, gerçek kişi yine kendi kimliğini doğrulatamıyor | Kurbanın talebiyle çözecek bir akış, "bu numara gerçekten senin" sorusunu cevaplamayı gerektirir — yani tam olarak bugün elimizde olmayan şeyi: gerçek bir kimlik kanıtı. Bilgi temelli kanıtla yazılacak bir "itiraz" akışı, saldırganın kurbanın bağını koparmasına da yarardı. Bugünkü kazanç yine de gerçek: geri alınamaz bir işlem geri alınabilir hâle geldi ve elle veritabanı müdahalesi gerekmiyor | Gerçek kimlik sağlayıcısı (e-Devlet) entegre edilince — ADR-017 o an yeni bir ADR'ye devrediyor |
| 91 | **ÇEREZ BANDI, RIZASI OLMAYAN ZİYARETÇİDE `/hesap-silindi` SAYFASININ YASAL BİLDİRİMİNİ ÖRTÜYOR** — bant `sticky` ve dar ekranda 449px'lik görünür alanın 229px'ini kaplıyor; "Saklananlar ve sebebi" başlığı altında kalıyor (2026-08-10'da tarayıcıda ÖLÇÜLDÜ, `elementFromPoint` ile doğrulandı) | Bant adım 17'nin bileşeni ve HER sayfada çiziliyor; onu değiştirmek bu adımın kapsamı dışıydı (CLAUDE.md §7: aynı anda tek modül). Pratik risk düşük: hesabını silen kullanıcı siteyi zaten kullanmıştır, yani bandı çoktan kapatmıştır; üstelik aynı liste silme ONAYINDAN ÖNCE de gösteriliyor, yani m.12/1-c bildirimi tek yere bağlı değil. Yine de "kullanıcı kapatmadı" senaryosunda bildirim görünmüyor | Bant ele alınacak ilk adımda — ya banda kapatma sonrası odak yönetimi eklenir ya da yasal bildirim taşıyan sayfalarda banda alt boşluk verilir. ⚠️ Aynı bant TÜM sayfalarda aynı davranışı gösteriyor; düzeltme tek yerde |
| 92 | ⛔ **PERSONEL DOĞRULAMASI CANLIDA TAMAMLANAMIYOR** — tohum personel adresleri `@ornek.test` uzantılı (`.test` IANA'nın rezerve ettiği alan adı, oraya posta teslim EDİLEMEZ) ve Resend'de doğrulanmış alan adımız yok (borç #25). Yani production'da kod hiçbir zaman ulaşmıyor ve kimse YENİ personel yetkisi alamıyor | **Bu bir gerileme DEĞİL, güvenli tarafa kapanma.** Adım 17c'den önce yetki yanlış bir kanıtla (kimlik numarası) dağıtılıyordu; şimdi doğru kanıt isteniyor ama o kanıt bu ortamda üretilemiyor. Mevcut personel hesapları (tohumun işveren verisinden bağladıkları) etkilenmiyor — hastane ve spor salonu onlarda çalışmaya devam ediyor. Sınır kullanıcıdan GİZLENMİYOR: `/personel-dogrulama` ekranı bunu açıkça yazıyor (`demoNotice`), yani kimse gelmeyecek bir kodu beklemiyor | Borç #25 ödenince (alan adı satın alınıp Resend'de doğrulanınca) ve tohum personel adresleri gerçek bir alan adına taşınınca. ⚠️ İkisi birlikte ödenmeli; yalnızca biri yetmez |
| 93 | **`/api/staff-verifications` gönderim hatasını kullanıcıya SÖYLEMİYOR** — kanal `unavailable` dönse bile yanıt "gönderildi" diyor | Şifre sıfırlamada tam tersi karar verildi (orada `unavailable` kullanıcıya söyleniyor) ve o karar orada doğru: arıza NADİR. Burada ise borç #92 yüzünden `unavailable` production'ın BEKLENEN durumu; söylenseydi "sağlayıcı hatası aldım demek ki bu adres rehberde ve boşta" diyen KALICI bir hesap sayımı kanalı açılırdı. Arıza yutulmuyor — `email-transport.ts` sunucu log'una yazıyor | Borç #92 ödenince bu karar gözden geçirilir: gönderim gerçekten çalışır hâle geldiğinde `unavailable` yeniden nadir bir arıza olur ve şifre sıfırlamadaki davranışa dönülebilir |
| 94 | **`SimulationCodeNotice` bileşeni ÜÇ KOPYA** — `VerificationPanels.tsx`, `PasswordResetCompleteForm.tsx` ve `StaffVerificationForm.tsx` içinde birebir aynı bileşen duruyor (DRY ihlali) | Ortak bir bileşene çıkarmak kayıt ve şifre sıfırlama akışlarına dokunmayı gerektiriyordu; CLAUDE.md §7 aynı anda tek modül diyor. Kopya yalnızca local/preview'da çizilen bir kutu, davranışı yok | Bu üç akıştan herhangi birine dokunulacak ilk adımda — `src/features/otp/components/SimulationCodeNotice.tsx` altına taşınır |
| 95 | **`EMAIL_MAX_LENGTH` yalnızca YENİ şemada uygulanıyor** — kayıt akışının e-posta alanında hâlâ üst sınır yok | Adım 17c kayıt akışına dokunmadı (CLAUDE.md §7). Pratik risk düşük: Zod'un `email()` doğrulaması zaten çok uzun girdileri biçim olarak eler ve gövde boyutu Next tarafında sınırlı | Kayıt akışına dokunulacak ilk adımda — `registrationContactSchema` içindeki `z.email()` zincirine `.max(EMAIL_MAX_LENGTH)` eklenir |
| 86 | **Hastane randevusundaki BRANŞ bilgisi özel nitelikli veri sayılabilir** — `14-privacy-and-compliance.md` "sağlık verisi hiç toplanmaz" diyor, oysa "Kardiyoloji randevusu aldı" kaydı sağlıkla ilişkilidir | Adım 17'de aydınlatma metni yazılırken fark edildi. Branş tek başına bir teşhis değil ve randevuyu kullanıcıya gösterebilmek için başka yol yok — randevu ekranı branşsız çalışamaz. KVKK m.6 açık rıza veya bir istisna ister; bugün hukuki sebep "sözleşmenin ifası" olarak yazıldı ve durum aydınlatma metninde AÇIKÇA kullanıcıya söylendi (gizlenmedi) | Gerçek kişisel veriyle çalıştırma kapısı açılmadan önce (ADR-017 üçlüsüyle birlikte) — ya randevu için ayrı açık rıza alınır ya da branş, randevu kaydından ayrılıp yalnızca doktor kimliğinden türetilir |
| 96 | **Cron izleyicisi (Sentry Cron Monitors) KURULAMADI** — `automaticVercelMonitors` seçeneği `vercel.json`'daki planlı görev için Sentry'de bir izleyici oluşturuyor ve beklenen koşu gelmediğinde haber veriyor; bu proje için tam da eksik olan şey | ⛔ **DENENDİ, ÇALIŞMADI ve yapılandırmadan KALDIRILDI.** Seçenek SDK'da `webpack.` altında, oysa bu proje **Turbopack** ile derleniyor (`npm run build` çıktısı: "Next.js 16.2.12 (Turbopack)") — Turbopack'te derleme zamanı enstrümantasyonu çalışmıyor, yani seçenek SESSİZCE etkisiz kalırdı. Yapılandırmada bırakmak "alarmım var" sanmak demekti; cron körlüğü bu projeye zaten bir kez pahalıya patladı | Sentry panelinden **elle** bir cron izleyicisi oluşturulur (kod gerektirmiyor), ya da Sentry Turbopack desteği ekleyince seçenek geri konur. ⚠️ O güne kadar "cron bugün çalıştı mı" sorusunun tek cevabı hâlâ `audit_logs` tablosu |
| 97 | **İstek kimliği (`requestId`) log satırlarına OTOMATİK girmiyor** — `readRequestId()` yardımcısı var (`x-vercel-id` başlığını okuyor) ama çağıran tarafından elle geçilmek zorunda; bugün hiçbir yer geçmiyor | Standart §12 her log satırında istek kimliği istiyor. Otomatik yapmanın yolu `AsyncLocalStorage` + bir ara katman kurmaktı; bu, HER route'a dokunmayı gerektirirdi ve CLAUDE.md §7 "aynı anda tek modül" diyor. Bugünkü kayıp sınırlı: Sentry olayları kendi istek bağlamını zaten taşıyor, eksik olan yalnızca stdout satırlarının birbirine bağlanması | Ara katman (middleware) yazılacak ilk adımda — borç #10'un (nonce tabanlı CSP) çözümü de bir ara katman gerektiriyor, ikisi aynı işte birleşir |
| 98 | **Kaynak haritası yüklemesi Turbopack'te DOĞRULANMADI** — `SENTRY_AUTH_TOKEN` local'de yok, yükleme hiç denenmedi | Yükleme Next 16'nın `runAfterProductionCompile` kancasından geçiyor (SDK'da `handleRunAfterProductionCompile` dosyası var), yani Turbopack'te de çalışması BEKLENİYOR — ama ölçülmedi ve ezberden "çalışıyor" denmiyor. Çalışmazsa hata yine yakalanır, yalnızca Sentry'deki yığın izi küçültülmüş (okunması zor) görünür | Vercel entegrasyonu kurulup ilk production dağıtımı yapılınca: Sentry'ye düşen ilk olayın yığın izinde `src/...` dosya adları görünüyor mu diye BAKILIR |
| ~~99~~ | ~~**CI iş akışlarındaki action'lar etiketle sabitlenmiş, tam commit SHA'sıyla DEĞİL**~~ | **ÖDENDİ (2026-08-12, adım 18d):** 11/11 `uses:` satırı tam commit SHA'sına çevrildi, yanına okunabilir `# vX.Y.Z` yorumu yazıldı. ⛔ **Major sürüm BİLEREK yükseltilmedi** — `actions/checkout` v7'ye çıkmıştı, ama sabitleme işine sürüm yükseltmesi karıştırmak iki ayrı riski tek commit'e sıkıştırmak olurdu; her action kendi major'ının en son SHA'sına sabitlendi. ⚠️ **Kaydın "her iş akışına `permissions:` bloğu eklenir" kısmı ZATEN YAPILMIŞTI** (ölçüldü: `ci.yml:14`, `e2e.yml:12` — ikisinde de `contents: read`), yani gerçek iş kaydın söylediğinin yarısıydı. Kural bir MEKANİZMAYA bağlandı: `tests/unit/workflows.test.ts` etiketli bir `uses:`, eksik sürüm yorumu veya eksik `permissions:` bloğu gördüğünde kırmızıya döner (mutasyonla kanıtlandı) | — |
| ~~100~~ | ~~**DEPO HERKESE AÇIK OLDUĞU HÂLDE SIR TARAMASI VE PUSH KORUMASI KAPALI**~~ **ÖDENDİ (2026-08-11)** — `gh api repos/:owner/:repo` çıktısı (2026-08-11): `secret_scanning: disabled`, `secret_scanning_push_protection: disabled`, `visibility: public` | `05-auth-security.md` "`.env` asla commit edilmez" diyor ama bunu uygulayan hiçbir otomasyon yok — kural bir niyet olarak duruyor. Açık bir depoya yanlışlıkla itilen anahtar botlar tarafından dakikalar içinde taranır; push koruması böyle bir itmeyi HENÜZ OLMADAN durdurur. Bugüne kadar sır sızmadı, yani kayıp gerçekleşmedi — ama koruma da yok | ✅ **2026-08-11'de AÇILDI** (`gh api` ile, proje sahibinin isteğiyle): `secret_scanning`, `secret_scanning_push_protection`, ayrıca `vulnerability_alerts` ve `dependabot_security_updates`. Çıktıyla doğrulandı. ⛔ Ders `05-auth-security.md`'ye yazıldı: **bir kural, mekanizması olmadan niyettir** |
| 101 | **`429` yanıtları `Retry-After` başlığı taşımıyor** — `grep` ile ölçüldü (2026-08-11): kaynak ağacında tek bir `Retry-After` yok, oysa hız sınırı beş ayrı akışta uygulanıyor | `03-api-guidelines.md` bu adımda güncellendi ve başlığı zorunlu kıldı. Ne zaman tekrar deneyeceğini bilmeyen istemci ya hemen tekrar dener (sınırı büyütür) ya da gereğinden uzun bekler. Bugünkü kayıp sınırlı: kullanıcıya gösterilen Türkçe mesaj zaten "biraz sonra tekrar deneyin" diyor, yani İNSAN yönü karşılanmış durumda — eksik olan MAKİNE yönü ve bu, mobil istemci geldiğinde asıl önemli hâle gelir | Hız sınırı katmanına dokunulacak ilk adımda — `RateLimitError` kalan süreyi zaten biliyor, `fail()` onu başlığa yazacak şekilde genişletilir |
| 102 | **Hata yanıtı istek kimliği taşımıyor** — istemciye giden gövdede yalnızca `code` ve `message` var; sunucu log'undaki satıra bağlayan bir değer yok | Borç #97'nin istemci bacağı ve onunla birlikte ödenmesi gerekiyor: kimliğin yanıta yazılabilmesi için önce log tarafında otomatik üretiliyor olması lazım. `03-api-guidelines.md` bu adımda güncellendi. Bugünkü kayıp: kullanıcı "hata aldım" dediğinde doğru log satırı ancak zaman aralığından tahmin edilerek bulunuyor — gerçek kullanıcı 0 olduğu için henüz kimseye pahalıya patlamadı | Borç #97 ile AYNI işte (ara katman + `AsyncLocalStorage`). Ayrı ödenmeye çalışılırsa kimlik yine elle taşınmak zorunda kalır |
| 103 | **API sürümlenmemiş ve kırıcı değişiklik politikası fiilen uygulanmıyor** — uçlar `/api/<kaynak>` biçiminde, sürüm segmenti yok; `Deprecation`/`Sunset` başlıkları hiç kullanılmıyor | Bugün API'nin tek tüketicisi kendi web arayüzümüz ve o, sunucuyla AYNI deploy'da güncelleniyor — yani sözleşmeyi bozmanın bedeli şu an sıfır. Sürüm segmentini bugün eklemek hiç kimseyi korumayan bir tören olurdu (CLAUDE.md §5.2 YAGNI). `03-api-guidelines.md` bu adımda güncellendi ve kuralı YAZDI; uygulaması ilk gerçek ikinci tüketiciyle başlar | ⛔ **Adım 19 (mobil uygulama) başlamadan ÖNCE.** O günden itibaren kullanıcının telefonundaki eski sürüm güncellenemez ve kaldırılan her uç, güncellemeyi almamış herkes için çökme demektir. Sürümleme kararı ADR ile alınır |
| 104 | **Uygulama düzeyinde istek gövdesi boyut sınırı yok** — hiçbir uç `Content-Length` kontrol etmiyor, sınır yalnızca platformdan geliyor | Vercel Fonksiyonları gövdeyi **4,5 MB**'de kesiyor ve `413 FUNCTION_PAYLOAD_TOO_LARGE` döndürüyor (2026-08-11'de resmî dokümandan doğrulandı), yani risk sınırsız değil. Ama sınır bizim değil sağlayıcının kararı: sağlayıcı değişirse veya bir uç kendi sunucumuzda koşarsa koruma kaybolur. Ayrıca 4,5 MB bir JSON gövdesi için hâlâ çok yüksek — Zod ancak gövde okunup ayrıştırıldıktan SONRA çalışıyor, yani maliyet zaten ödenmiş oluyor | Ara katman yazılacak ilk adımda (borç #10 · #97 · #102 ile aynı iş) — JSON uçları için kaynak bazlı, dosya yükleme için ayrı bir üst sınır tanımlanır |
| 105 | ⛔ **AYNI HATA KODU İKİ FARKLI HTTP DURUMU DÖNÜYOR** — `BOT_CHECK_FAILED` (auth'ta 403, destekte 400) · `PAYMENT_DECLINED` (ödemede 409, üyelikte 402) · `INSUFFICIENT_FUNDS` (ödemede 409, üyelikte 402). 2026-08-11'de adım 18b'de belge çıkarılırken kaynak taranarak bulundu | `03-api-guidelines.md` `code`'un "makine için SABİT" olmasını istiyor; aynı kodun akışa göre farklı durum dönmesi bunu bozar. `code` üzerinden dallanan bir istemci, hangi akışta olduğuna göre farklı durum kodu görür. Bugün tek tüketici kendi arayüzümüz ve o `code` ile dallanıyor, durumla değil — bu yüzden görünür bir arıza YOK. Düzeltmek durum kodunu değiştirmek demek, yani **kırıcı bir değişiklik**; üstelik dört ayrı özelliğin hata modülüne dokunmayı gerektiriyor (CLAUDE.md §7). ⭐ Sayının BÜYÜMESİ engellendi: `api-docs-catalog.test.ts` bu üçünü bilinen istisna olarak tanıyor, dördüncüsü eklenirse CI kırmızıya döner | Ödeme ve üyelik akışlarına dokunulacak ilk adımda — ya kodlar ayrıştırılır (`MEMBERSHIP_PAYMENT_DECLINED`) ya da durum kodları tek değere çekilir. `03-api-guidelines.md`'nin yeni "sözleşme ömrü" bölümü uygulanır: kırıcı değişiklik tek adımda yapılmaz |
| 106 | **İKİ UÇTA YOL PARAMETRESİ ZOD'DAN GEÇMİYOR** — `/api/carts/current/items/{itemId}` (yalnızca `.trim()` kontrolü) ve `/api/memberships/{membershipId}` (hiç kontrol yok). Belge çıkarılırken fark edildi | `03-api-guidelines.md` "her endpoint girişi (body, query, **params**) Zod ile doğrulanır, **istisna yok**" diyor. Pratik risk bugün düşük: değer parametreli sorguya bağlanıyor (enjeksiyon yok) ve sahiplik kontrolü ayrıca yapılıyor, yani bulunamayan kimlik `404` dönüyor. Ama kural ihlali gerçek ve biçim doğrulaması olmayan bir alan, ileride bir önbellek anahtarına veya log satırına girdiğinde sorun çıkarır. ⭐ İhlal belgede GİZLENMEDİ: `/api/docs` bu iki parametrenin yanında "bugün Zod şemasından geçmiyor" yazıyor | Sepet veya üyelik akışına dokunulacak ilk adımda — diğer uçlardaki `recordId` deseni uygulanır (tek satırlık iş) |
| 107 | **YANIT GÖVDELERİNİN ŞEMASI BELGELENMEDİ** — `/api/docs` istek tarafını gerçek Zod şemalarından türetiyor ama yanıt tarafında yalnızca zarfı (`{ data }` / `{ error }`) ve Türkçe bir tarifi belgeliyor | Yanıtların Zod şeması YOK: `ok<T>()` jenerik, `T` route'tan geliyor ve çalışma anında okunamıyor. Elle yazılsaydı istek tarafının aksine **sapan bir iddia** olurdu — ve ADR-019'un tüm gerekçesi "sapan ikinci kaynak yaratma"ydı. Gerçek çözüm yanıtlara da Zod şeması vermek ve onları fiilen kullanmak (testte doğrulamak); bu 40 route'a dokunmayı gerektiriyor (CLAUDE.md §7). Bugünkü kayıp sınırlı: tek tüketici TypeScript tipleriyle zaten derleme anında bağlı | ⛔ **Adım 19 (mobil uygulama) başlamadan ÖNCE** — o gün istemci artık TypeScript tipleriyle bağlı olmayacak ve yanıt sözleşmesinin tek kaydı belge olacak. Borç #103 (sürümleme) ile aynı işte birleşir |
| 108 | ⛔ **İLK YÜK JS BÜTÇESİ AŞILIYOR — 281,7 KB gzip, hedef 200 KB** (2026-08-11'de adım 18c'de ölçüldü; local üretim yapısı, en ağır sayfa `/restoran`. Canlıda 288,8 KB). Ağırlığın ~150 KB'ı **Sentry'nin tarayıcı SDK'sı**: en büyük parça 132 KB gzip ve içi Sentry ile dolu, yanında 18 KB'lık ikinci bir parça daha var | Oturum tekrarı ve izleme zaten KAPALI (adım 18a), yani bu kullanılan bir özelliğin bedeli değil — **paketten atılamayan ölü kod**. En olası sebep borç #96: derleme Turbopack ile yapılıyor ve Sentry'nin ağaç budama seçenekleri (`__SENTRY_TRACING__` gibi) SDK'da `webpack.` altında duruyor. **Emin değilim — doğrulanmadı.** Kapı bugün cırcır değeriyle (288 KB) kurulu: bugünden kötüye gidiş merge edilemiyor ama hedef tutulmuyor. Kapıyı 200 KB ile kurmak CI'ı ilk günden kırmızı yapar ve hiçbir şey merge edilemezdi | Sentry yapılandırmasına dokunulacak ilk adımda (borç #96 ile AYNI iş — ikisi de Turbopack/Sentry sorusu). Önce ÖLÇÜLECEK: budama açılabiliyor mu, açılınca kaç KB düşüyor. Düşmüyorsa karar "Sentry tarayıcı tarafında kalsın mı" sorusuna döner ve ADR gerektirir |
| 109 | **LCP kapısı GERÇEK AĞI ölçmüyor** — CI localhost'a bakıyor ve Chrome'un ağ kısıtı yalnızca ALT KAYNAKLARA uygulanıyor, ana belgeye uygulanmıyor (2026-08-11'de ölçüldü: en yavaş kaynak 32 ms → 15 059 ms, ama TTFB 10 ms → 6 ms yani değişmiyor). Sunucu da aynı makinede | Aynı sayfanın LCP'si local'de 588 ms, **canlıda 2844 ms** ölçüldü — yani kapının gördüğü sayı gerçek kullanıcının gördüğü sayı değil. Kapı yine de değersiz değil: bir REGRESYON TELİ olarak çalışıyor ve CLS'i gerçekten ölçüyor. Ama "bütçeyi tutuyoruz" demiyor ve dosyada bu açıkça yazılı (`tests/quality/budget.ts`). ⛔ Bugün canlı anasayfa 2844 ms ile hedefin (2500 ms) ÜSTÜNDE ve bunu CI göremiyor | Preview dağıtımına karşı ölçen ayrı bir kapı kurulacak ilk adımda — PR'ın preview URL'i `PLAYWRIGHT_BASE_URL` ile zaten verilebiliyor. ⚠️ O kapı ağ gürültüsüne açık olur; eşik tek koşuya değil birkaç koşunun ortancasına bağlanmalı. Borç #108 ödenirse bu sayı da kendiliğinden düşer (JS'in yarısı Sentry) |
| ~~110~~ | ~~**Değişiklik günlüğünde adım 17c, 18a ve 18b'nin girdisi YOK**~~ — `docs/project/CHANGELOG.md` adım 17b'den doğrudan 18c'ye atlıyor (2026-08-11'de adım 18c'de fark edildi) | Üç adımın da kendi PR'ı, roadmap satırı ve commit gövdesi var; yani bilgi kaybolmadı, yalnızca tek bir yerde toplanmadı. `10-definition-of-done.md` günlüğü tamamlanma kapısı sayıyor, dolayısıyla bu üç adımda o kapı fiilen atlanmış. Geriye dönük yazmak bu adımın kapsamı dışıydı (CLAUDE.md §7: aynı anda tek modül) ve üç adımı hatırdan yazmak, günlüğü kaynak olmaktan çıkarır | **ÖDENDİ (2026-08-12, adım 18d):** üç girdi de yazıldı ve kaydın istediği gibi **commit gövdelerinden** üretildi (`b74ea67` · `6712d32` · `821d035`), hatırdan değil. Günlük artık 17b→17c→18a→18b→18c→18d kesintisiz |
| 111 | **Lisans testi KANONİK METİNDEN SAPMAYI yakalamıyor** — `tests/unit/license.test.ts` başlığı, telif satırını ve beyanla eşleşmeyi ölçüyor ama gövdenin birebir doğru olduğunu ölçmüyor | Bu boşluk ÖLÇÜLEREK bulundu: MIT metnindeki `OUT OF,` → `OUT OF OR` bozukluğu aylarca durdu ve yeni yazılan test onu da yakalamazdı (2026-08-11, adım 18c). Değiştirilmiş bir MIT metni artık MIT değildir; GitHub ve otomatik lisans tarayıcıları kanonik metne göre eşleştirir, sapan metin "other/unrecognized" olarak görünür. Bugünkü kayıp sınırlı çünkü metin şu an DÜZELTİLDİ ve doğru — eksik olan, bir daha bozulursa yakalayacak mekanizma | Lisans dosyasına dokunulacak ilk adımda — SPDX'in kanonik MIT metni bir sabite konur ve test gövdeyi (telif satırı hariç) birebir karşılaştırır. ⚠️ Satır sarma farkları normalleştirilmeli, yoksa test biçimlendirmeye kırmızı döner |
| 112 | **`style-src` hâlâ `'unsafe-inline'` içeriyor** — `script-src` adım 18d'de nonce'a geçti ama stil tarafı geçemedi | ⛔ **TAHMİN DEĞİL, ÖLÇÜLDÜ:** nonce'lu hâli fiilen kuruldu, üretim yapısı derlendi, tarayıcıda ölçüldü. İki şey kırıldı: (1) `next/image` `fill` modunda görsele `style` ÖZNİTELİĞİ yazıyor — `/market`'te 45 görselin `getComputedStyle().position` değeri `absolute` yerine `static` çıktı; nonce yalnızca ETİKETE takılabilir, özniteliğe takılamaz (`style-src-attr` mimari sınırı). (2) `sonner` çalışma anında 14 859 karakterlik nonce'suz bir `<style>` enjekte ediyor, `style.sheet` boş ölçüldü ve `ToasterProps` arayüzünde `nonce` alanı YOK. ⛔ `'nonce-…'` ile `'unsafe-inline'`i yan yana yazmak çözüm DEĞİL: CSP'ye göre nonce varken `'unsafe-inline'` yok sayılır. ⛔ **EN SİNSİ TARAF: kırılma tarayıcı KONSOLUNA HİÇBİR ŞEY YAZMADI** — "konsol temiz" burada "çalışıyor" demek değildi. Risk farkı önemli: script tarafındaki `'unsafe-inline'` doğrudan kod çalıştırma demekti ve O KAPANDI; stil tarafında kalan risk görsel kurcalama ve dar bir sızdırma kanalı | `next/image` inline style'ı bırakan bir sürüme geçilirse veya `sonner` nonce desteklerse. Ayrıca `style-src-attr 'unsafe-inline'` + `style-src 'nonce-…'` ayrımı denenebilir — ama sonner'ın `<style>` etiketi yine kırılır, yani tek başına yetmez |
| 113 | ⛔ **CANLIDA TURNSTILE BULMACASI ÇİZİLMİYOR** — `/kayit` sayfasında konteyner var (71,5 px) ama içi boş: yalnızca `<input type="hidden" name="cf-turnstile-response">`, iframe YOK (2026-08-12'de merge SONRASI production'da ölçüldü) | ⭐ **CSP KAYNAKLI DEĞİL — ÖLÇÜLEREK ELENDİ.** Sıkı CSP altında, production'da, Cloudflare'ın resmî test anahtarıyla (`1x00000000000000000000AA`) `window.turnstile.render()` çağrıldı: **`callback` tetiklendi ve jeton üretildi** (`XXXX.DUMMY.TOKEN.XXXX`). Yani politika Turnstile'ın çalışmasını engellemiyor. Cloudflare belgesi de doğruluyor: gereken tek şey `script-src` + `frame-src` (ikisi de var) ve *"Turnstile works with strict-dynamic"*. Ayrıca production'da `110200` (domain not authorized) hatası YOK — ana alan adı yetkili. ⚠️ Geriye kalan tek fark GERÇEK SİTE ANAHTARI: onunla ne iframe çiziliyor ne jeton geliyor. ⛔ **Bu merge ile GELMEDİ:** merge öncesi production dağıtımı (`kac9fr5h5`) da aynı boş konteyneri gösteriyor — ama o karşılaştırma tek başına kesin değil, çünkü o alt alan adı `110200` alıyor | ⛔ **SONRAKİ OTURUMUN İLK İŞİ.** Sıra: (1) Cloudflare Turnstile panelinde site anahtarının **widget modunu** kontrol et — "Invisible"/"Managed" ise kutu zaten çizilmez ve davranış DOĞRU olabilir; (2) Vercel'deki `NEXT_PUBLIC_TURNSTILE_SITE_KEY` panel değeriyle aynı mı; (3) Hostname Management'ta `benim-belediyem.vercel.app` yazılı mı. ⚠️ **Bu çözülene kadar kayıt akışının canlıda uçtan uca çalıştığı VARSAYILMAMALI** — sunucu boş jetonu reddediyor |
| 114 | ⚠️ **Turnstile önizleme ortamında ÇALIŞMIYOR** — preview alan adı Cloudflare Hostname Management listesinde yok (hata `110200`, 2026-08-12'de ölçüldü) | Bot koruması preview'da denenemiyor: kayıt, giriş, şifre sıfırlama ve destek formları o ortamda bulmacasız kalıyor. Production etkilenmiyor (alan adı yetkili ve kutu görünüyor). Ayrıca preview'da bulmacasız kalmak bir güvenlik açığı değil — o ortam `noindex` ve gerçek kullanıcıya kapalı | ⚠️ **İSTEĞE BAĞLI PANEL İŞİ, zorunlu değil:** Cloudflare Turnstile panelinde `*.vercel.app` (veya preview alan adı) Hostname Management'a eklenirse preview'da da çalışır. Yapılmazsa yalnızca preview'da bulmaca denenemez |
